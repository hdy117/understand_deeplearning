# 第 8 章：衡量性能（Measuring Performance）

> 书：《Understanding Deep Learning》Ch.8  
> 前置：第 5–7 章已经能把训练损失压下去。  
> 本章：训练损失往下走，是否等于模型真的学会了？期望测试误差从哪三处来？  
> 后续：零训练损失的解不唯一时，第 9 章在数据沉默处注入偏好。

---

## 0. 训练集上的满分不是判决

原书用 MNIST-1D 把这件事摆上台面：十个数字的一维模板，随机变形再加噪，采 40 个水平位置，得到 \(D_i=40\) 的输入。两隐层、每层 100 个单元的全连接网，SGD、batch 100、学习率 0.1，多类交叉熵。大约 4000 step 之后，训练误差掉到 0，训练损失逼近 0。

这不表示分类器完美。模型可能把这 4000 个点背下来了。换一份没见过的图，错误率仍然可以很高。第 2 章已经把 underfitting / overfitting 当作直线模型的两种病；本章要问的是更硬的一句：

> **一个在训练集上近乎满分的模型，为什么仍可能在新数据上很差？那份误差能拆成哪些彼此不同的来源？**

调查链：

```text
train 可以满分
        │
        ▼
真正要小的是未知总体 p(x,y) 上的期望损失，不是已见样本上的成绩
        │
        ▼
独立 test set 用来估计前者
        │
        ▼
平方损失下，期望测试误差 = Noise + Bias² + Variance
        │
        ▼
更多数据通常压 Variance；更大容量通常压 Bias，经典区域里却可能抬高 Variance
        │
        ▼
插值阈值附近测试误差先升，再加大容量又降（double descent）
        │
        ▼
看不见真实 Bias / Variance → validation 选超参数，test 只估一次最终流程
```

三项分解是平方损失下的恒等式，不是对所有损失都成立的加法。Double descent 是经验现象。把这两层揉成一句「模型越大越容易过拟合」，假矛盾就来了。

---

## 1. 世界不是训练集

现实按未知联合分布产生数据 \((X,Y)\sim p(x,y)\)。训练集只是一次有限抽样：

\[
D=\{(x_i,y_i)\}_{i=1}^{I}.
\]

学习算法读 \(D\)，吐出模型 \(f_D\)。下标 \(D\) 是整章的灵魂：**换一份训练集，通常得到另一个模型。**

真正要小的是 **population risk**：

\[
R(f_D)
=
\mathbb E_{(X,Y)\sim p}
\bigl[\ell(f_D(X),Y)\bigr].
\]

它同时依赖模型、目标分布和损失。0–1 损失下它是错误概率；平方损失下它是平均平方误差，也是后面三项分解的舞台；交叉熵下它是对真实标签的平均负对数概率。

训练损失是同一份 \(\ell\) 在 \(D\) 上的平均，叫 **empirical risk**。它不是 \(R(f_D)\) 的中立估计：最小化它的那个 \(f_D\)，已经看见了这些点。把训练损失当成绩单，等于让考生出题。

所以需要一份从未进入训练的 **test set**，用它上面的平均损失去估计 \(R\)。前提是 test 和未来部署来自同一 \(p\)。分布一变，旧分数就不再代表你真正关心的总体。

MNIST-1D 把这句话变成图：训练曲线可以贴地，测试曲线停在半空。记忆不是理解。第 20 章还会用随机打乱的标签把「能记住」再演示一遍；本章先把它当作诊断的起点，而不是奇闻。

---

## 2. 测试误差里有两层随机

固定一个输入 \(x\)，仍有两件会抖的事：

1. 世界在这个 \(x\) 上仍可能吐出不同的 \(Y\)；
2. 换一份训练集，\(f_D(x)\) 会变。

于是出现四个对象：

```text
Y  --Noise-->  μ(x)  --Bias-->  f̄(x)  --Variance-->  f_D(x)
```

条件均值 \(\mu(x)=\mathbb E[Y\mid X=x]\) 是「这个输入处世界的平均规律」。平均模型 \(\bar f(x)=\mathbb E_D[f_D(x)]\) 是「把所有可能训练集都拿来训一遍，学习系统平均会报什么」。一次真实的 \(Y\) 和一次真实的 \(f_D\)，分别绕着这两个平均值抖。

三问，必须钉在同一个 \(x\) 上：

- 让现实再开一次，\(Y\) 会不会变？会 → Noise。
- 换一份 \(D\) 重训，\(f_D(x)\) 会不会变？会 → Variance。
- 把无数次训练的预测平均掉，\(\bar f(x)\) 是否仍偏离 \(\mu(x)\)？会 → Bias。

输入本身一直在变，就分不清抖动来自哪一层。

---

## 3. 平方损失下的恒等式

先只对测试输出取期望。把 \(f_D(x)-Y\) 写成 \((f_D(x)-\mu(x))+(\mu(x)-Y)\)，平方，交叉项在 \(\mathbb E[Y\mid x]=\mu(x)\) 时消失：

\[
\mathbb E_{Y\mid x}\bigl[(f_D(x)-Y)^2\bigr]
=
\bigl(f_D(x)-\mu(x)\bigr)^2
+
\sigma^2(x),
\]

其中 \(\sigma^2(x)=\mathrm{Var}(Y\mid x)\)。第一项还含训练数据的随机性。再把 \(f_D-\mu\) 写成 \((f_D-\bar f)+(\bar f-\mu)\)，对 \(D\) 取期望，又一个交叉项因 \(\mathbb E_D[f_D-\bar f]=0\) 消失。原书的加法（8.7）是：

\[
\boxed{
\mathbb E_{D,Y\mid x}\bigl[(f_D(x)-Y)^2\bigr]
=
\underbrace{\mathbb E_D\bigl[(f_D(x)-\bar f(x))^2\bigr]}_{\mathrm{Variance}}
+
\underbrace{\bigl(\bar f(x)-\mu(x)\bigr)^2}_{\mathrm{Bias}^2}
+
\underbrace{\sigma^2(x)}_{\mathrm{Noise}}
}
\]

再对 \(X\) 取期望，就是总体 MSE。人话：

```text
期望测试 MSE
  = 世界自己会抖
  + 学习系统平均瞄歪了
  + 这一次抽到的练习册让模型偏离了自己的平均状态
```

交叉项消失不是魔法：\(\mu\) 和 \(\bar f\) 本来就是各自那一层随机性的平均值。

**离散小宇宙：两项相加真的等于左边。** 固定一个 \(x\)。世界只吐两个标签，各一半：\(Y\in\{0,2\}\)，所以 \(\mu=\mathbb E[Y]=1\)，\(\sigma^2=\mathrm{Var}(Y)=1\)。学习算法只可能抽到两份训练集 \(D_1,D_2\)，各一半；训完预测 \(f_{D_1}(x)=3\)，\(f_{D_2}(x)=1\)。于是 \(\bar f=(3+1)/2=2\)。

Bias²：

\[
(\bar f-\mu)^2=(2-1)^2=1.
\]

Variance：

\[
\mathbb E_D[(f_D-\bar f)^2]=\tfrac12\bigl((3-2)^2+(1-2)^2\bigr)=1.
\]

Noise：\(\sigma^2=1\)。右边合计 \(1+1+1=3\)。

左边：四个等可能结局 \((D,Y)\)，平方误差

| \(D\) | \(f_D\) | \(Y\) | \((f_D-Y)^2\) |
|---|---|---|---|
| \(D_1\) | 3 | 0 | 9 |
| \(D_1\) | 3 | 2 | 1 |
| \(D_2\) | 1 | 0 | 1 |
| \(D_2\) | 1 | 2 | 1 |

平均 \(12/4=3\)。恒等式不是口号，是同一张 \(2\times 2\) 表的两种加法。

三项的常用来源不同。Noise：真随机、标错、当前 \(x\) 里没记下的解释变量——相对**现在这份输入表示**不可约，补特征可能把一部分「噪声」变成可预测。Bias：模型族太弱、架构不对、算法系统性偏离。Variance：数据少、模型对样本过敏。

一个容易黏住的点：Noise 限制的是期望 **test** MSE，不阻止把 **train** MSE 压到零。训练时几乎不会再看见同一个 \(x\)，网络可以给每个训练点单独开一个坑。满分训练和噪声地板同时成立。

另一个：这里的 Bias 是 **statistical bias**（平均模型离 \(\mu\) 多远），不是第 9–13 章的 **inductive bias**（学习系统在数据没说话的地方偏爱哪类函数）。一个是误差账，一个是选择机制。

这项加法对平方损失成立。分类的 0–1 或交叉熵里，三股力量仍在，但不保证能这样干净地拆开相加。

---

## 4. 两只旋钮，以及它们互相拆台

Noise 是地板，相对当前任务改不了。另外两项可以动。

**更多数据通常压 Variance。** 两份训练集越来越像同一总体的两次抽样，\(f_D\) 就围着 \(\bar f\) 抖得更小。这是最无聊也最有效的药。

**更大容量通常压 Bias。** 第 3 章的折线段数随 hidden units 增加；三段的网贴不上准正弦，十段就可以。平均而言，\(\bar f\) 离 \(\mu\) 更近。

经典区域里第二只旋钮有副作用：容量变大，模型开始拟合这一份 \(D\) 里的噪声，换一份 \(D\) 曲线就换一个形状，Variance 上升。训练误差继续降，测试误差不一定降。这就是 **bias–variance trade-off**，也是 overfitting 的精确图像：描述力用来建模噪声，而不是 \(\mu\)。

Underfitting 是 Bias 太大（族太硬，直线拟合波浪）。Overfitting 是 Variance 太大（族太软，把这一份抽样的起伏写进方程）。两句话不要混写成「模型不好」。

在画成 U 型的那张图上，存在一个「不太偏、也不太抖」的容量。实践里看不见 \(\mu\)，也就看不见这两项，U 型只能当故事，不能当选宽度的公式。

优化失败是第四个桶：容量够，梯度没走到。先看训练损失是不是已经该降的都降了，再把剩余误差记到 Bias 或 Variance 头上。第 6–7 章的学习率和初始化坏了，三项分解帮不上忙。

---

## 5. Double descent：U 型右边还有一半

回到 MNIST-1D，把隐层加宽，画训练/测试误差对参数量。训练误差很快贴零。参数量等于训练点数的那条竖线，并不是记忆发生的精确位置——网络往往在那之前就已经把训练集背完。测试误差先降，在**刚好能记完**附近抬起一个峰，然后随着容量继续增加再次下降。这就是 **double descent**。

两件事叠在一起。第一件是经典 trade-off：刚够记忆时，模型必须扭曲自己才能穿过每个训练点，点与点之间的预测很狂，Variance 爆掉。第二件更怪：训练损失已经是零，再加容量帮不上拟合训练点，只能改变**点与点之间**的函数。模型在数据沉默处偏爱哪一类解，叫 **inductive bias**。

高维里训练点极稀。40 维、每维 10 档就是 \(10^{40}\) 个格子，10000 个点几乎什么都没钉住。插值阈值之后，问题从「能不能穿过这些点」变成「穿过之后在空格子里走哪条路」。一种说得通的图像是：刚够拟合时只能扭曲；更宽之后，同一组点之间存在更平滑的穿过方式，而常见的训练路径又碰巧偏爱它们。原书同时画了反例：过参数模型**可以**造出不平滑的插值，它只是有能力平滑，没有义务平滑。Double descent 没有推翻三项分解，推翻的是「Variance 随容量单调升」这条经验。

---

## 6. 所以才需要 validation

经典区域里你看不见 Bias 和 Variance；现代区域里你也不知道再加多少容量测试误差才会停。容量来自层数、宽度、尚未出场的架构选择；学习率、batch size 同样会改测试成绩。这些不进反传的量统称 **hyperparameters**。

做法不是在 test 上挑它们。对 test 调参，test 就变成另一份训练，估计不再诚实。引入第三份 **validation set**：

```text
每一组超参数
    → 只在 train 上拟合
    → 在 validation 上比较
    → 留下 validation 最好的那一套
    → 用从未见过的 test 估一次最终流程
```

超参数空间比 \(\phi\) 小，仍大到不能穷举：许多是离散的（几层），有的还互相条件（第十层有多宽，前提是真有十层）。不能对它们做第 6 章那种梯度下降，只能按已有结果聪明地抽样。每试一组都要完整训一遍，所以贵。数据很少时可用 K-fold 降低单次划分的偶然性；最终 test 仍应隔离。

反复看 test 再改模型，自适应选择已经发生，test 事实上就是 validation。第 5 章的损失是训练用的；本章的三份数据是评价协议。两件事不要共用同一份点。

**协议草图（数字）。** 100 个点：70 train / 15 val / 15 test。两组超参数，只在 train 上拟合，只在 val 上比：

```text
split:   [ 70 train | 15 val | 15 test ]
                    ^ pick α here
                               ^ look once at the end

α=0.1  →  val error 0.22
α=0.01 →  val error 0.18   ← keep this
then    →  test error 0.19  (one number; do not go back)
```

若拿 test 再比一次 \(\alpha\)，你等于把 15 个 test 点变成第二份 val：报告的 \(0.19\) 会乐观。K-fold 只是把中间那 15 轮换着用，**最外层 test 仍然只能看一次**。

---

## 7. 不要把别的曲线塞进三项加法

语言模型常见

\[
L(N,D)\approx L_\infty+A/N^\alpha+B/D^\beta.
\]

它可以和「任务地板、模型受限、数据受限」做类比，却不是从本章 MSE 分解推出来的：那边是明确假设下的恒等式，这边是 held-out 交叉熵的经验拟合，\(N\) 和 \(D\) 对误差来源也不独立。把它当「第四项」只会把恒等式弄脏。Diffusion 会不会背下训练集，是第 14、18 章的事。

---

## 8. 自测

1. 为什么 \(f_D\) 必须带下标 \(D\)？去掉它会藏起哪一层随机性？  
2. Population risk 和 empirical risk 差在对谁取平均？为什么后者不是前者的中立估计？  
3. Test set 估计的是什么？它的分布一旦和部署环境不同，分数还代表什么？  
4. 固定同一个 \(x\)，用三问区分 Noise、Bias、Variance。  
5. 平方展开后，两个交叉项为什么会在取期望时消失？  
6. Noise 为什么能限制期望 test MSE，却不阻止 train MSE 到零？  
7. Statistical bias 和 inductive bias 各在问哪句话？  
8. 更多数据、更大容量，经典区域里各自主要动哪一项？为什么第二只旋钮会拆台？  
9. Interpolation threshold 为什么不必恰好等于「参数个数 = 样本数」？  
10. Double descent 推翻了三项分解，还是推翻了 Variance 的单调经验？平滑插值是定理还是假说？  
11. 为什么超参数不能在 test 上选？Validation 和 test 的逻辑地位差在哪？  
12. 训练损失很高时，为什么第一反应不该是「Variance 太大，上正则」？

---

## 9. 合上书再看一眼

训练损失是考生自己出的题。真正要小的是未知 \(p(x,y)\) 上的期望损失；独立 test 用来估计它，前提是还代表未来。平方损失下，这份期望拆成世界自己会抖、学习系统平均瞄歪、以及这一份练习册引起的抖动。更多数据压第三项；更大容量压第二项，经典区域里却可能把第三项抬起来。插值之后零损失解不唯一，测试误差可以再降——那是点与点之间的 inductive bias 在选路，不是三项加法被推翻。

现实看不见 \(\mu\) 和 \(\bar f\)，所以用 validation 选超参数，用隔离的 test 估一次最终流程。下一章接住剩下的问题：许多函数都能把训练损失压到零，学习系统凭什么选其中一个。
