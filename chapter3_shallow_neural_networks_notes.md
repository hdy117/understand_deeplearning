# 第 3 章：浅层神经网络（Shallow Neural Networks）

> 书：《Understanding Deep Learning》Ch.3  
> 前置：第 2 章用直线走完「函数族 → 损失 → 下山」。直线描述不了弯曲。  
> 本章：在线性中间插入非线性，得到 piecewise linear 的浅层网络；够宽时可以逼近任意连续函数。  
> 后续：浅层够宽就够表达，为什么还要深？第 4 章回答。

---

## 0. 直线不够弯，就在中间折一下

第 2 章的模型永远是一条直线。真实的 \(x\mapsto y\) 很少那么乖。本章仍然写 \(y=f(x,\phi)\)，仍然用损失和下山，只改函数族：先对输入做几次线性，用激活函数折一下，再线性地加起来。

得到的是 **连续 piecewise linear** 函数。折点的位置、各段斜率、整体高度，都由参数决定。母问题：

> **这族折线从哪来？「够宽就能逼近任意连续函数」究竟保证了什么，又故意没保证什么？**

调查链：

```text
直线太硬
        │
        ▼
线性 → 非线性激活 → 再线性
        │
        ▼
每个 hidden unit 贡献一个「关节」
        │
        ▼
关节够密，局部看起来都像直线 → 宽度版 UAT
        │
        ▼
输入/输出都可以是向量；多输出共享关节位置
        │
        ▼
激活必须非线性，否则整网塌回一条直线
        │
        ▼
浅层已经够表达 → 第 4 章问为什么还要叠层
```

---

## 1. 一个十参数的例子，把折线拆开

标量到标量、三个 hidden units 的浅层网络：

\[
y
=
\phi_0
+
\phi_1\,a(\theta_{10}+\theta_{11}x)
+
\phi_2\,a(\theta_{20}+\theta_{21}x)
+
\phi_3\,a(\theta_{30}+\theta_{31}x).
\]

三步，不要当成一套神秘电路：

1. 对 \(x\) 做三次线性：\(\theta_{d0}+\theta_{d1}x\)；
2. 各自经过激活 \(a(\cdot)\)；
3. 用 \(\phi_1,\phi_2,\phi_3\) 加权求和，再加偏移 \(\phi_0\)。

最常用、也最好画的激活是 **ReLU**：

\[
a(z)=\mathrm{ReLU}(z)
=
\begin{cases}
0 & z<0,\\
z & z\ge 0.
\end{cases}
\]

负的被剪成 0，正的原样通过。换一组十个参数，折点位置、各段斜率、整体高度一起变；函数族仍然是 piecewise linear，成员变了。

钉死一组十个数，折线就可以用手算出来。取

\[
\begin{aligned}
\theta_{10}&=-1,&\theta_{11}&=1,\\
\theta_{20}&=0,&\theta_{21}&=1,\\
\theta_{30}&=1,&\theta_{31}&=-1,\\
\phi_0&=0,&\phi_1&=2,&\phi_2&=-1,&\phi_3&=1.
\end{aligned}
\]

三个 pre-activation 是 \(x-1\)、\(x\)、\(1-x\)。ReLU 的关节在它们过零处：\(x=1\)、\(x=0\)、\(x=1\)。第三个和第一个撞在同一竖线，所以看起来像两个关节、三段。对三个探针 \(x=-1,0.5,2\)：

| \(x\) | \(h_1=\mathrm{ReLU}(x-1)\) | \(h_2=\mathrm{ReLU}(x)\) | \(h_3=\mathrm{ReLU}(1-x)\) | \(y=2h_1-h_2+h_3\) |
|---|---|---|---|---|
| \(-1\) | \(0\) | \(0\) | \(2\) | \(2\) |
| \(0.5\) | \(0\) | \(0.5\) | \(0.5\) | \(0\) |
| \(2\) | \(1\) | \(2\) | \(0\) | \(0\) |

\(x=-1\) 时只有 \(h_3\) 活着；\(x=0.5\) 时 \(h_2,h_3\) 活着；\(x=2\) 时 \(h_1,h_2\) 活着。同一套 \(\theta,\phi\)，换 \(x\) 只是换哪几个 ReLU 被剪掉。

第 2 章的手续原封不动：知道 \(\phi\) 就能 inference；有训练对就能写 least-squares \(L[\phi]\)；训练仍是找 \(\hat\phi\)。变的只是族变大了，损失曲面不再是「碗底唯一」的那口锅——第 6 章会碰到非凸。

---

## 2. Hidden unit：一个关节从哪来

把中间量写出来：

\[
h_1=a(\theta_{10}+\theta_{11}x),\quad
h_2=a(\theta_{20}+\theta_{21}x),\quad
h_3=a(\theta_{30}+\theta_{31}x),
\]

\[
y=\phi_0+\phi_1 h_1+\phi_2 h_2+\phi_3 h_3.
\]

每个 \(h_d\) 先是一条直线，ReLU 在过零处把它的负半轴剪掉。三条直线过零的位置，成为最终折线的三个 **joints**。剪完的三条再按 \(\phi_d\) 缩放，最后由 \(\phi_0\) 决定整体高度。

每一段线性区域对应一种 **activation pattern**：谁被剪掉（inactive），谁还活着（active）。某一段若只有 \(h_1,h_3\) 活跃，斜率就是 \(\theta_{11}\phi_1+\theta_{31}\phi_3\)——源斜率和后边权重的乘积，加起来。

上一节那组参数把这句话变成算术。开区间 \((0,1)\) 里 \(x-1<0\)、\(x>0\)、\(1-x>0\)，所以只有 \(h_2,h_3\) 活跃。这一段的斜率是

\[
\theta_{21}\phi_2+\theta_{31}\phi_3=(1)(-1)+(-1)(1)=-2.
\]

核对：\(x=0.25\) 时 \(h=(0,0.25,0.75)\)，\(y=2\cdot 0-0.25+0.75=0.5\)；\(x=0.75\) 时 \(h=(0,0.75,0.25)\)，\(y=-0.75+0.25=-0.5\)。两点差 \(\Delta x=0.5\)、\(\Delta y=-1\)，斜率 \(-2\)，和公式一致。\(x>1\) 时变成 \(h_1,h_2\) 活跃，斜率 \(\theta_{11}\phi_1+\theta_{21}\phi_2=2-1=1\)。关节把数轴切开，每一刀两边换一套「谁活着」，斜率就跳到另一组乘积和。

三个 hidden units 最多四个线性区域。第四段斜率不是独立的：要么全 inactive 斜率为 0，要么是其他段斜率的组合。加一个 hidden unit，通常就加一个关节。网络图画成左输入、中 hidden、右输出；每条边一个参数。偏置常常不画，ReLU 也不画，但方程里它们都在。

原书把带偏置的 \(\phi_0+\sum\phi_i h_i\) 仍叫 linear，严格说是 affine。机器学习里两者混用；本书跟这个习惯。没有激活、或激活本身是线性，整条链叠在一起仍是一条直线，第 2 章的模型就没被真正扩大。

---

## 3. Universal approximation：够宽就能逼近，不等于能找着

\(D\) 个 hidden units：

\[
h_d=a(\theta_{d0}+\theta_{d1}x),
\qquad
y=\phi_0+\sum_{d=1}^{D}\phi_d h_d.
\]

\(D\) 是浅层网络的 **capacity**。ReLU 下最多 \(D\) 个关节、\(D+1\) 段。段越来越短，每一小段都更像直线，弯曲的目标函数就可以被折线贴上去。

**宽度版 universal approximation theorem：** 对紧集上的任意连续函数、任意指定精度，存在一个单隐层、有限 hidden units 的网络，能逼近到这个精度。Cybenko（sigmoid 类激活）和 Hornik（更宽的非线性激活类）证明了存在性。

两件它没说：

- 没说 hidden units 要多少才「够」——对有些函数，这个数可以大到没法用；
- 没说训练找不找得到那些参数。存在一条折线贴上去，和下山能走到那组 \(\phi\)，是两件事。

第 4 章会利用第一件：同样参数预算下，深度往往能造出多得多的线性区域。第 6–7 章处理第二件。

---

## 4. 输出是向量：关节位置被共享

每个输出用 **同一组** hidden units 的不同线性组合。一个输入、四个 hidden、二维输出：

\[
y_1=\phi_{10}+\sum_{d=1}^{4}\phi_{1d}h_d,
\qquad
y_2=\phi_{20}+\sum_{d=1}^{4}\phi_{2d}h_d.
\]

关节由 ReLU 剪在哪决定，所以 \(y_1(x)\) 和 \(y_2(x)\) 的折点在同一批竖直线上；各段斜率和整体高度可以不同。这不是实现偷懒，是结构：共享 hidden 等于共享对输入空间的剖分，输出只在每块剖分上换一组线性系数。

---

## 5. 输入是向量：关节变成超平面

两个输入 \(x=(x_1,x_2)^\top\) 时，每个 hidden unit 吃一个平面：

\[
h_d=a(\theta_{d0}+\theta_{d1}x_1+\theta_{d2}x_2).
\]

ReLU 把平面的负半边剪掉，得到一块折过的平面。再线性组合，输出是连续 piecewise linear 的曲面，区域是凸多边形。每个多边形对应一种激活模式。

更高维时，每个 hidden unit 的「关节」是一张超平面，把空间分成该 unit 活跃/不活跃两侧。线性区域变成凸多面体。若 hidden units 数 \(D\) 等于输入维 \(D_i\)，并且超平面对齐坐标轴，\(D_i=1\) 分成 2 段，\(D_i=2\) 分成 4 象限，\(D_i=3\) 分成 8 卦限，一般是 \(2^{D_i}\) 个 orthants。浅层网络通常 \(D>D_i\)，区域数还要多。Zaslavsky（1975）：\(D\) 张超平面剖分 \(D_i\) 维空间，区域数最多

\[
\sum_{j=0}^{D_i}\binom{D}{j}.
\]

经验上浅层网的区域数大致落在 \(2^{D_i}\) 和 \(2^{D}\) 之间。\(D=500\)、\(D_i=100\) 这种以今天标准很小的网，区域数就可以超过 \(10^{107}\)。表达力在高维涨得极快；「有没有足够多的折」很少是浅层网的真正瓶颈。

---

## 6. 一般式，以及那些不得不认识的词

\(x\in\mathbb{R}^{D_i}\rightarrow y\in\mathbb{R}^{D_o}\)，\(D\) 个 hidden units：

\[
h_d
=
a\Bigl(\theta_{d0}+\sum_{i=1}^{D_i}\theta_{di}x_i\Bigr),
\qquad
y_j
=
\phi_{j0}+\sum_{d=1}^{D}\phi_{jd}h_d.
\]

参数 \(\phi=\{\theta_{\bullet\bullet},\phi_{\bullet\bullet}\}\)。ReLU 下，输入空间被剖成凸多面体，每个多面体里是一段线性函数；剖分对每个输出相同，多面体里的线性系数可以不同。

| 词 | 在方程里指什么 |
|----|----------------|
| Input / hidden / output layer | 左、中、右三列 |
| Neuron / hidden unit | \(h_d\) |
| Pre-activation | 进 ReLU 之前的线性值 |
| Activation | 出 ReLU 之后的 \(h_d\) |
| Weight | 斜率参数（边上的数） |
| Bias | 偏置 \(\theta_{d0},\phi_{j0}\) |
| Fully connected | 一层每个单元连到下一层每个单元 |
| Feed-forward | 连接成无环图，数据单向走 |
| MLP | 至少一层 hidden 的网络（历史名字） |
| Shallow / deep | 一层 hidden / 多层 hidden |

「Neural」来自节点密连的图看起来像神经元。原书明确说：和大脑计算的类比薄弱，往后按函数想，不要按生物学想。

激活函数必须非线性。历史常用 logistic sigmoid / tanh，大输入时导数饱和，训练不稳定。ReLU 在正半轴导数恒为 1，是现代能训起来的重要零件（第 7 章）。它的毛病是负半轴导数为 0：若某 ReLU 对所有训练样本都吃负数，进来的权重就再也走不动，叫 **dying ReLU**。Leaky / parametric / concatenated ReLU、GELU、Swish、HardSwish 都在修这件事。本书其余部分坚持普通 ReLU，因为线性区域数最好数。

Dying 不是修辞。设某个 hidden unit \(h=\mathrm{ReLU}(-2+0\cdot x)=\mathrm{ReLU}(-2)=0\)，对所有训练 \(x\) 都如此——偏置已经把整条 pre-activation 按在零下。平方损失对这个 unit 的权重 \(\theta_{d1}\) 的梯度要经过 \(\mathrm{ReLU}'(-2)=0\)，链式法则乘上 \(0\)，\(\theta_{d1}\) 这一步的更新是 \(0\)。偏置 \(\theta_{d0}=-2\) 同样吃到 \(0\) 梯度，除非有别的路径（没有）。于是这个关节从函数上消失了：少一个折，容量偷偷掉了一档。训练点若全在 \(x\in\{0,1,2\}\)，一个 \(\theta_{d0}+\theta_{d1}x=-5-3x\) 的 unit 同样永远 inactive。Leaky ReLU 把负半轴改成 \(0.01z\)，同一处导数变成 \(0.01\) 而不是 \(0\)，门没锁死——这就是「修」的全部：留一条很窄的路，让这个 unit 还有机会爬回正半轴。

---

## 7. 自测

1. 把浅层网络的计算拆成三步。哪一步去掉之后，模型会塌回第 2 章的直线？  
2. ReLU 的关节在什么位置？三个 hidden units 最多几段线性区域？第四段斜率为什么不独立？  
3. 某一段只有 \(h_1,h_3\) 活跃时，斜率怎样用 \(\theta,\phi\) 写出来？  
4. Capacity 在浅层网里用什么量衡量？加一个 hidden unit，通常多了什么？  
5. UAT 保证「存在一组参数」，还是保证「下山找得到」？  
6. 两个输出共享 hidden units，折点位置为什么必须相同？哪些量仍可以不同？  
7. 二维输入时，线性区域的形状是什么？每个区域对应什么 activation pattern？  
8. 为什么 \(D=D_i\) 且超平面对齐坐标轴时，区域数是 \(2^{D_i}\)？浅层网通常比这个更多还是更少？  
9. Pre-activation 和 activation 差在哪一步？Weight 和 bias 分别是方程里哪一类参数？  
10. 激活若改成 \(a(z)=\psi_0+\psi_1 z\)，从输入到输出还剩什么非线性？  
11. Dying ReLU 是哪一种局部平坦？它和第 7 章要处理的「梯度消失」是不是同一句话？  
12. 为什么本书后面仍然用普通 ReLU，而不是一上来就换 Swish？

---

## 8. 合上书再看一眼

浅层网络只比直线多一次折：线性、ReLU、再线性。每个 hidden unit 往函数上钉一个关节；关节把输入空间剖成凸块，每块里仍是线性。输出可以有很多个，但它们共用同一份剖分。

够宽的浅层网在紧集上能逼近任意连续函数。这句话解决的是「族够不够大」，不是「参数好不好找」，也不是「同样多的参数能造多复杂的折」。后一个缺口在高维尤其刺眼：有些函数让浅层需要指数多的 hidden units。下一章把浅层网络的输出再送进另一个浅层网络，看深度怎样用折叠把同一段函数复制出许多份。
