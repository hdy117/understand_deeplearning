# 第 20 章：深度学习为什么能成？（Why Does Deep Learning Work?）

> 书：《Understanding Deep Learning》Ch.20  
> 前置：第 2–19 章已经把模型、损失、训练、结构先验和生成走完。按理，非凸、过参数、维数灾难，不该既好训又好泛化。  
> 本章：这两件「不该发生的事」分别被哪些因素托住？哪些只是现象，还没有处方理论。  
> 后续：第 21 章不再问机制，而问责任。

---

## 0. 按教科书直觉，这件事不该成功

MNIST-1D 只有 40 维输入、10 维输出。两层全连接，每层大约 43 个 hidden units（约 4000 参数），就能把 10000 个训练点分类到满分，并且对新点还不离谱。我们现在把「够宽就能拟合、拟合了还能泛化」当成天气。原书要你先觉得奇怪。本章和其他章不一样：它几乎没有新算法，只把已经用过的零件翻过来问为什么没塌。

**训练不该容易。** 任意非凸函数的全局最小是 NP-hard，某些神经网络损失也是。算法却很少明显卡在坏的局部最小或鞍点上，还能把多余容量招募去拟合还没被解释的训练点。过参数也许能解释一部分，但故事不干净：AlexNet 约 6000 万参数、约 100 万张图，可每张又被 2048 种 augmentation 放大；GPT-3 1750 亿参数、3000 亿 token。两者都不显然是「参数远多于独立数据」，却都能训起来。

**泛化更不该。** 维数灾难：MNIST-1D 的 40 维若每维只量化成 10 档，输入空间有 \(10^{40}\) 个格子，训练点只是其中的 \(10^{-36}\)。网络描述的函数也极复杂：两隐层、宽 400 的全连接可以有 \(10^{42}\) 个线性区域，每个训练点大约分到 \(10^{38}\) 个——绝大多数区域训练时从未见过数据，却仍表现得合理。更刺眼的是第 8 章已经见过的现象：参数越多，泛化往往越好。多余的自由度按理可以在训练点之间胡来。

母问题：

> **数据、模型、算法里究竟哪一部分有特殊结构，才让「能拟合」和「能泛化」同时发生？过参数和深度是必要的，还是只是让当前算法好走？**

调查链：

```text
非凸 + 维数灾难 → 按理既不好训也不该泛化
        │
        ▼
随机标签也能拟合 → 不只是「真实函数简单」
        │
        ▼
显式正则不是必要条件；过参数、激活、lottery ticket 更像主因
        │
        ▼
损失：一大族等价最小、轨迹走低维子空间、坏局部最小很少
        │
        ▼
泛化：谁被选中（SGD、flatness、权重范数、架构）
        │
        ▼
剪枝 / 蒸馏能缩小，却还没做出真正欠参数的 SOTA
        │
        ▼
深度：表达、好训、归纳偏置三条假说，没有一条被钉死
```

原书把「全局最小」用得很松：训练点全对就算。我们不知道别处还有没有更低的损失。

---

## 1. 拟合为什么走得通：先排除几条诱人的假说

**不是因为真实函数简单。** 完全随机的 \(28\times 28\) 二值图到十类，只能靠背下 \(2^{784}\) 个赋值。MNIST 却很好训。Zhang et al.（2017）把 CIFAR-10 的标签打乱再训 AlexNet：仍然能把训练集拟合到近乎满分。真实结构会帮泛化，但「能拟合」本身不依赖标签有结构。随机数据上损失下降得更慢，说明结构会影响优化路径，不是能不能到位。

**不是因为 L2 或 dropout 把曲面变凸了。** 同一实验里，去掉这两样仍然能拟合随机标签。隐式正则（第 9 章有限步长）还在，可是步长加大并不会让拟合变容易，和「步长越大隐式正则越强」对不上。

**SGD 的「在谷之间跳」不是完整故事。** 第 6 章说过 mini-batch 噪声可能帮忙逃逸。全 batch 也能把许多网训到位；更宽、更深的网往往更好拟合（MNIST-1D 上原书图 20.2）。噪声有用，但不是开关。

剩下的更像主因。

**过参数。** 解不再是草堆里的一根针，而是针堆（Sejnowski）。几乎总有一个方向还能把损失再压低一点。实践里网络常过参数一到两个数量级；augmentation 让「参数/数据」比说不清，因为增强不是独立新点。第 8 章也显示：没到过参数，许多网已经能完美拟合——过参数是充分条件里很强的一项，不是全部。

**激活函数。** Sigmoid / tanh 在尾巴上几乎常数，梯度接近 0，改进模型的机制被掐断。ReLU 在一半输入上斜率是 1，leaky ReLU 在整个实轴上都有斜率。第 3、7 章把 ReLU 当好画的折线；本章把它当成「训练信号还活着」的条件。

**初始化。** Xavier / He 对深层是为了不让激活和梯度指数爆炸或消失（第 7 章）。把 He 再放大若干倍，仍然常常能训到，只是更慢。初始化更像「把你放到可走的盆地入口」，不是单独创造了那个盆地。

**深度对拟合是双刃。** 极深会炸梯度、碎梯度（第 7、11 章），多半是数值问题，没有证据表明损失随深度本质更凸或更凹。随机标签的 MNIST 上，更深的网有时用更少 iteration 就拟合——可能只是梯度更陡，或 He 把浅而宽的网放得离解更远。

**Lottery ticket。** Frankle & Carbin：把小网（如 VGG）训完，剪掉幅度最小的权重，从**同一组初始值**再训，成绩可以持平甚至更好；随机重初始化同一拓扑则不行。大网里藏着可训的小子之网，训练是在抽中一张彩票。过参数的一种读法因此更细：有效的是「有多少张彩票」，而不只是参数个数。深度会不会在固定参数预算下改变彩票数量，还没有精确刻画。

---

## 2. 损失曲面看起来不像教科书的坑坑洼洼

**最小点多到可以换衣。** 全连接一层里的 hidden units 可以连同进出权重一起置换，输出不变。卷积的通道同样。ReLU 前的权重乘 \(\alpha>0\)、后面除 \(\alpha\)，函数不变（第 4 章非负齐次性）。BatchNorm 把均值方差重置，又多一簇冗余。这些是「对所有输入输出都一样」的等价。过参数之后，只要求训练点上输出对即可，等价族更大。

**通往最小的路几乎是直的。** Goodfellow et al.：从初始化到训完连一条直线，损失沿这条线常常单调下降（开头可能有个小包）。真的优化轨迹不是直线，但 Li et al. 发现它躺在低维子空间里——大而近乎凸的区域很早抓住轨迹，只沿少数方向漏斗下去。更怪的是：把优化限制在一个随机的低维仿射子空间里，网络常常还能训。

**最小点之间是连着的。** 两个独立找到的最小，直线连接上损失可能鼓起来；改走非线性的低损失通道，往往能连上。不是一口口孤立的井。

**曲率：越低的临界点，向下弯的方向越少。** 随机高斯场有这个性质；神经网络损失上，鞍点的负特征值数目随损失下降而减少。浅层网的分析里甚至可以只有鞍点、没有坏的局部最小。Fort & Scherlis 在随机方向上测曲率，也支持「坏坑不多」。这不是证明，是一组互相咬合的现象：下山不太会被坑骗，因为坑很少，而且底部是一大片连着的盆地。

---

## 3. 泛化：过参数之后，是谁在选那个解

过参数意味着训练过程的细节决定停在等价族的哪一个成员上。有些细节稳定地改变泛化。

**算法。** SGD 通常比全 batch GD 更能泛化。SGD vs Adam 曾经被说成 SGD 更强，更新的研究认为差距没那么大、取决于调参。Batch size 和 learning rate 的比会动泛化：batch 变大往往伤泛化，学习率变大往往帮泛化——第 9 章隐式正则的亲戚，不是新定理。

**Flat minima。** 从 Hochreiter & Schmidhuber 起就有猜想：宽而平的最小比尖的更能泛化。直观：参数估不准一点，损失不会炸。MDL 也说：宽的最小用更少 bit 就能描写权重。度量可以是等损失区域的体积、二阶曲率、或随机扰动后损失涨多少。反例和测量依赖坐标尺度的批评都在；「flat \(\Rightarrow\) 泛化」不能当无条件定理，只能当和 batch size / 学习率现象对得上的一条假说。

**权重范数的 Goldilocks 带。** Fort & Scherlis 还看到：\(\ell_2\) 范数落在某段区间时，曲率异常地正，泛化也好。范数间接连着 Lipschitz：太小，函数跟不上真实变化；太大，训练点之间抖得过分。Liu et al. 用这件事解释 **grokking**（Power et al.）：训练误差早已为零，测试准确率过很多 epoch 才突然爬起来。假说是一开始范数太大，点与点之间乱抖；隐式或显式正则把范数慢慢拖进那条带子，泛化才突然出现。它和第 8 章 double descent 是亲戚：都在说「零损失之后还在选哪一个插值」，不是又一次拟合。

**架构。** 第 10–13 章的归纳偏置在这里重新到账：卷积假定网格上统计可平移，Transformer 适合内容路由，GNN 适合任意拓扑。换对架构，比同等规模的全连接泛化好得多。这是第 9 章「偏好写进结构」的终极形式：连 \(\lambda\) 都不必出现。

**过参数本身。** 第 8 章图 8.10：过参数程度上去，泛化往往继续变好，叠上经典 U 型就是 double descent。假说仍是「更多自由度让点与点之间可以更平滑」。没有义务平滑——只是常见训练路径碰巧往那边走。

---

## 4. 对抗样本：流形附近，不是流形上

泛化故事讲的是训练点之间。沿输出对输入的梯度走一小步，人眼几乎看不出，类别却可以从狗翻成鸵鸟（Szegedy et al.）。这些点靠近数据、却不在数据流形上，叫 **adversarial examples**。

当前较好的解释不是「模型对训练分布外完全脆弱」，而是它在利用训练分布里**范数很小、人对它不敏感**的那些特征（Ilyas et al., 2019）。参数多没有自动变成对这种邻域的先验。第 8 章的「点与点之间表现合理」，漏掉的是沿梯度走一步的那条缝。

---

## 5. 必须那么多参数吗？

SOTA 在复杂数据上，几乎没有「参数明显少于训练点数」的例子。但第 1 节说过：参数多也让拟合变容易。于是分不清：小模型是表达不够，还是当前算法找不到好解。两套压缩手续在问这件事。

**Pruning。** 按二阶导数或更常见的按幅度扔掉权重，也可以扔 hidden unit、卷积通道、残差层，再 fine-tune，有时反复做。Han et al. 把 VGG 在 ImageNet 上收到约 8% 权重仍能维持成绩。模型小了，但 VGG 相对 ImageNet 训练数仍大约 100 倍（不算 augmentation）——这还不是「欠参数也行」。Lottery ticket 把剪枝读成架构搜索：中奖的是那组初始权重，不是剪完的拓扑本身。

**Knowledge distillation。** 小的 student 去模仿大的 teacher。Hinton et al. 强调类别之间的关系，让 student 去贴 teacher 的 pre-softmax logits。Zagoruyko & Komodakis 再对齐中间的空间激活图，用 18 层残差（约 1100 万参数）去逼 34 层（约 6300 万）的 ImageNet 成绩——仍然多于约 100 万张训练图。后来的方法可以再压，还没有令人信服的证据表明真正欠参数的模型能到 SOTA。

**理论下界。** Bubeck & Sellke（2021）：\(D\) 维里，平滑插值需要的参数大约是「仅仅插值」的 \(D\) 倍。按这个尺子，ImageNet 上的现有模型可能还不够过参数。

暂时结论很保守：**现有算法和现有数据尺度下，过参数像是泛化需要的；剪枝和蒸馏能缩小，却还没翻案。**

---

## 6. 必须那么深吗？

宽度版 UAT 说浅层也能任意逼近（第 3 章）。历史上 ImageNet 成绩随深度涨，直到训不动；残差和 BatchNorm（第 11 章）把更深的门打开，成绩再涨。ViT、GPT-3、DALL·E-2 都是几十到几百层。

反例也在：WideResNet 用更浅但更宽的残差追上 ResNet；Goyal et al. 用并行卷积通道、大约 12 层就接近更深网；Veit et al. 发现残差网里真正干活的多是 5–17 层的短路径。即便如此，分类成绩像样的网几乎都超过 10 层。没有定论为什么。原书留下三条假说，要分开记账。

**表达更复杂。** 第 4 章：同样参数，深层线性区域多得多；还存在浅层需要指数宽度才能逼近的函数。不知道真实任务是不是那种函数。

**更好训。** 也许浅而够宽的网也能达到 SOTA，只是找不到又拟合、又平滑插值的那个解。Urban et al. 把 16 个 CIFAR-10 卷积网的 ensemble 蒸进不同深度的 student：固定参数预算，浅层复制不了 teacher，成绩随深度升。这支持「深度在帮优化」，不是只在帮表达。

**更好的归纳偏置。** 卷积和 Transformer 把局部共享、再一层层收到全局，写成的函数族本来就不是「任意函数」。Ulyanov et al. 的 deep image prior：未训练的 CNN 结构本身就能当去噪、超分的先验。Frankle et al. 随机初始化卷积核并冻住，只训 BatchNorm 的平移和缩放，分类仍能不错。Zhang et al. 的随机卷积特征也能撑起后面的 kernel 分类。Urban 的蒸馏里，蒸进卷积架构系统性地好过蒸进全连接——局部到全局这条链，浅层全连接学不像。

三条可以同时有一点真。现有算法下又大又深更好用；更小更浅的网往往存在，只是更难找到，也更难被写进同样的结构约束里。

---

## 7. 自测

1. 为什么「能把训练集拟合到满分」按计算复杂度不该容易？Zhang 的随机标签实验排除了哪条假说？  
2. AlexNet 和 GPT-3 怎样让「过参数」这句话变得不干净？  
3. 维数灾难和线性区域数，各自怎样说明泛化不该发生？  
4. Lottery ticket 的手续是什么？为什么「剪完再随机初始化」会失败？  
5. ReLU 相对 sigmoid，在「训练信号还活着」这句话里差在哪？  
6. 置换 hidden units、缩放 ReLU 两侧权重、BatchNorm，各自制造了哪一类等价最小？  
7. 初始化到解的直线上损失单调，和「真轨迹走低维子空间」是同一件事吗？  
8. Flat minima 的直观是什么？为什么它不能写成定理？  
9. 权重范数的 Goldilocks 带怎样同时碰到 Lipschitz 和 grokking？  
10. 对抗样本按 Ilyas 的解释，利用的是训练分布里的什么？这和第 8 章「点与点之间合理」差在哪条缝？  
11. VGG 收到 8% 权重仍能分类，为什么还不能说「欠参数也行」？  
12. Distillation 让 student 去贴 teacher 的哪一层信息？为什么 18 层逼 34 层仍不是欠参数反例？  
13. Bubeck & Sellke 的 \(D\) 倍参数在比较哪两种插值？  
14. 深度的三条假说（表达 / 好训 / 归纳偏置）各对应哪类证据？Deep image prior 支持的是哪一条？

---

## 8. 合上书再看一眼

深度学习按先验不该既好训又好泛化：损失非凸，输入空间相对训练点大得荒谬，函数的折又多到绝大多数线性区域从未见过数据。它仍然成功。比较站得住的零件是过参数（针堆，以及针堆里的彩票）、ReLU 这类不饱和的激活、把参数放到可走盆地里的初始化，以及 SGD、权重范数和架构在等价最小里做的选择。损失看起来更像连着的盆地而不是孤立的坑。Grokking 和 double descent 都在说：零损失之后，点与点之间那条路还在被慢慢挑。对抗样本则指出，沿梯度走一步的邻域，并不在那条「合理插值」的承诺里。

剪枝和蒸馏能缩小，却还没做出参数少于数据的 SOTA；平滑插值甚至可能要求更多参数。深度有表达、优化、归纳偏置三条账，没有处方理论能事先算出某种数据、某种网会训成什么样。第 2–19 章教会的是怎么造系统；本章标明：系统能成，机制仍是一组互相咬合的现象。下一章换一个更硬的问题——这些系统一旦进世界，目标对齐的是谁。
