# 用语言模型理解最大似然：一步一步指南

## 前置知识：Categorical 分布

Categorical 分布用于描述：一个离散随机变量在 $K$ 个互斥类别中选择一个类别。

例如，假设下一个 token 只能是：

$$
\{\text{苹果},\text{香蕉},\text{橘子}\}
$$

模型认为它们出现的概率分别是：

$$
\boldsymbol p=(0.5,0.3,0.2)
$$

那么可以写成：

$$
X\sim\operatorname{Categorical}(0.5,0.3,0.2)
$$

这表示随机变量 $X$ 的一次取值只能是三个类别之一。Categorical 分布由概率向量参数化：

$$
\boldsymbol p=(p_1,p_2,\ldots,p_K)
$$

并满足：

$$
p_k\geq 0,
\qquad
\sum_{k=1}^{K}p_k=1
$$

### 概率质量函数与负对数似然

如果类别使用编号 $1,2,\ldots,K$ 表示，那么：

$$
P(X=k\mid\boldsymbol p)=p_k
$$

实际观察到第 $y$ 类时，该观测的似然是 $p_y$，负对数似然为：

$$
L=-\log p_y
$$

因此，模型为真实类别分配的概率越高，损失越小。例如，真实类别是“苹果”时：

- 若 $P(\text{苹果})=0.9$，损失为 $-\log0.9\approx0.105$；
- 若 $P(\text{苹果})=0.1$，损失为 $-\log0.1\approx2.303$。

负对数似然会强烈惩罚“对真实类别信心很低”的预测。

### one-hot 表示与交叉熵

假设有三个类别，真实类别是第二类：

$$
\boldsymbol y=(0,1,0)
$$

模型预测：

$$
\boldsymbol p=(0.2,0.7,0.1)
$$

交叉熵为：

$$
L=-\sum_{k=1}^{K}y_k\log p_k
$$

由于 one-hot 向量只有真实类别的位置为 1：

$$
L=-(0\log0.2+1\log0.7+0\log0.1)=-\log0.7
$$

所以对于 one-hot 标签：

$$
\boxed{\text{Categorical 负对数似然}=\text{多分类交叉熵}}
$$

### 与 Bernoulli 和 Multinomial 的区别

- **Bernoulli 分布**描述一次二值结果，只需要参数 $p$，另一个结果的概率是 $1-p$。
- **Categorical 分布**描述一次从 $K$ 个互斥类别中选择一个类别。
- **Multinomial 分布**描述重复进行 $n$ 次 Categorical 试验后，各类别分别出现多少次。

语言模型在每个位置只预测一个下一个 token，因此使用 Categorical 分布，而不是 Multinomial 分布。Bernoulli 可视为二类别情形，但二分类模型通常使用单个 logit 和 Sigmoid，多分类模型通常使用 $K$ 个 logits 和 Softmax。

### 为什么需要 Softmax

神经网络输出的 $K$ 个 logits：

$$
\boldsymbol z=(z_1,z_2,\ldots,z_K)
$$

可以是任意实数，不能直接作为概率。Softmax 将它们转换为：

$$
p_k=\frac{\exp(z_k)}{\sum_{j=1}^{K}\exp(z_j)}
$$

转换后的概率非负且总和为 1。因此，Softmax 的作用就是把神经网络的无约束输出转换成 Categorical 分布所需的概率参数。

在语言模型中，$K$ 是词表大小。模型在每个位置都产生一个定义在整个词表上的 Categorical 分布，然后从中选择或采样一个 token。

---
## 1. 我们究竟要解决什么问题

语言模型的任务不是直接输出一句固定的话，而是给一段文本中的各种可能序列分配概率。

假设一句话由 $T$ 个 token 组成：

$$
x_{1:T}=(x_1,x_2,\ldots,x_T)
$$

语言模型希望建立整个序列的概率模型：

$$
p(x_1,x_2,\ldots,x_T)
$$

如果一句话符合训练语料中的语言规律，模型应该给它较高概率；如果一句话不自然，模型应该给它较低概率。

例如，一个训练良好的中文语言模型通常应该满足：

$$
p(\text{“今天天气很好”})
>
p(\text{“今天很好天气”})
$$

这就是语言模型的基本目标：学习自然语言序列的概率分布。

## 2. 第一步：确定输出及其概率分布

假设词表大小为 $V$。在每个位置 $t$，下一个 token 只能是词表中的某一个元素：

$$
x_t\in\{1,2,\ldots,V\}
$$

因此，下一个 token 是一个离散多分类变量，适合使用 Categorical 分布：

$$
x_t\mid x_{<t}
\sim
\operatorname{Categorical}(\boldsymbol p_t)
$$

其中：

$$
x_{<t}=(x_1,\ldots,x_{t-1})
$$

表示当前 token 之前的上下文，而：

$$
\boldsymbol p_t=(p_{t,1},\ldots,p_{t,V})
$$

是模型预测的词表概率向量，满足：

$$
p_{t,k}\geq 0,
\qquad
\sum_{k=1}^{V}p_{t,k}=1
$$

所以语言模型使用最大似然时，最基本的分布假设是：

> 给定此前的上下文，下一个 token 服从由神经网络参数化的类别分布。

## 3. 第二步：用链式法则分解序列概率

一句话中的 token 显然不是彼此独立的。例如看到“我喜欢吃”以后，“苹果”的概率通常会高于“桌子”。

因此，语言模型不能写成：

$$
p(x_{1:T})=\prod_{t=1}^{T}p(x_t)
$$

正确做法是利用概率的链式法则：

$$
p(x_{1:T})
=
\prod_{t=1}^{T}p(x_t\mid x_{<t})
$$

展开后为：

$$
p(x_1,x_2,\ldots,x_T)
=
p(x_1)
p(x_2\mid x_1)
p(x_3\mid x_1,x_2)
\cdots
p(x_T\mid x_1,\ldots,x_{T-1})
$$

这不是独立性假设，而是对任何联合概率分布都成立的链式法则。

自回归语言模型的特殊之处，是按照从左到右的顺序对这些条件概率进行建模。

## 4. 第三步：让神经网络预测分布参数

模型读取上下文 $x_{<t}$，然后为词表中的每个 token 输出一个实数：

$$
\boldsymbol z_t=f(x_{<t};\phi)
$$

这里：

- $\phi$ 是模型的全部参数；
- $\boldsymbol z_t\in\mathbb R^V$；
- $z_{t,k}$ 是第 $k$ 个 token 的 logit。

logit 可以是任意实数，不能直接作为概率。模型使用 Softmax 将其转换为 Categorical 分布的参数：

$$
p_{t,k}
=
p(x_t=k\mid x_{<t};\phi)
=
\frac{\exp(z_{t,k})}
{\sum_{j=1}^{V}\exp(z_{t,j})}
$$

因此，语言模型的概率建模过程是：

$$
x_{<t}
\xrightarrow{\text{神经网络}}
\boldsymbol z_t
\xrightarrow{\text{Softmax}}
\boldsymbol p_t
\xrightarrow{\text{Categorical}}
x_t
$$

神经网络并没有绕开概率模型。它负责预测概率分布的参数。

## 5. 第四步：写出训练语料的似然

假设训练集中有 $N$ 个序列，第 $n$ 个序列为：

$$
x^{(n)}_{1:T_n}
$$

模型赋予该序列的概率为：

$$
p(x^{(n)}_{1:T_n};\phi)
=
\prod_{t=1}^{T_n}
p(x^{(n)}_t\mid x^{(n)}_{<t};\phi)
$$

如果把不同训练序列视为独立样本，整个训练集的似然为：

$$
\mathcal L(\phi)
=
\prod_{n=1}^{N}
\prod_{t=1}^{T_n}
p(x^{(n)}_t\mid x^{(n)}_{<t};\phi)
$$

最大似然训练寻找如下参数：

$$
\hat\phi
=
\arg\max_\phi\mathcal L(\phi)
$$

直观地说，模型要让训练语料中实际出现的每一个下一个 token 都具有较高概率。

## 6. 第五步：得到负对数似然损失

似然包含大量概率乘积，数值可能极小。因此取对数：

$$
\log\mathcal L(\phi)
=
\sum_{n=1}^{N}
\sum_{t=1}^{T_n}
\log p(x^{(n)}_t\mid x^{(n)}_{<t};\phi)
$$

训练通常写成最小化问题，因此使用负对数似然：

$$
L(\phi)
=
-\sum_{n=1}^{N}
\sum_{t=1}^{T_n}
\log p(x^{(n)}_t\mid x^{(n)}_{<t};\phi)
$$

如果使用 one-hot 标签 $y_{t,k}$，其中真实 token 对应位置为 1，其余位置为 0，则单个位置的损失可以写成：

$$
L_t
=
-\sum_{k=1}^{V}y_{t,k}\log p_{t,k}
$$

由于只有真实 token 的 $y_{t,k}=1$，所以：

$$
L_t=-\log p_{t,x_t}
$$

这正是多分类交叉熵。因此，在标准自回归语言模型中：

$$
\boxed{
\text{token 交叉熵}
=
\text{Categorical 负对数似然}
}
$$

## 7. 一个可手算的例子

假设词表只有四个 token：

$$
\{\text{我},\text{喜欢},\text{苹果},\text{香蕉}\}
$$

训练序列是：

```text
我 喜欢 苹果
```

为了简化，暂时忽略句首和句尾 token。模型需要完成两个预测：

1. 给定“我”，预测“喜欢”；
2. 给定“我 喜欢”，预测“苹果”。

假设模型给出的概率如下：

| 上下文 | 真实下一个 token | 模型赋予真实 token 的概率 |
|---|---|---:|
| 我 | 喜欢 | 0.60 |
| 我 喜欢 | 苹果 | 0.70 |

模型赋予整个条件序列的似然为：

$$
\mathcal L
=
0.60\times0.70
=
0.42
$$

负对数似然为：

$$
L
=
-\log(0.60)-\log(0.70)
\approx
0.511+0.357
=
0.868
$$

假设另一个模型给出的真实 token 概率分别为 $0.9$ 和 $0.8$，则：

$$
\mathcal L'=0.9\times0.8=0.72
$$

$$
L'
=
-\log(0.9)-\log(0.8)
\approx
0.329
$$

第二个模型对训练序列赋予更高似然，因此具有更低损失。

## 8. 实际训练时的数据如何移动一位

假设完整序列为：

```text
<BOS> 我 喜欢 苹果 <EOS>
```

训练输入和标签通常错开一个位置：

| 位置 | 输入上下文最后一个 token | 预测目标 |
|---:|---|---|
| 1 | `<BOS>` | 我 |
| 2 | 我 | 喜欢 |
| 3 | 喜欢 | 苹果 |
| 4 | 苹果 | `<EOS>` |

从张量角度看：

```text
input_ids:  <BOS>  我    喜欢   苹果
labels:       我    喜欢  苹果   <EOS>
```

Transformer 使用因果注意力掩码，保证位置 $t$ 只能读取当前位置及其之前的 token，不能偷看未来标签。

## 9. Teacher forcing 是什么

训练时，模型在预测位置 $t$ 时使用真实的历史 token：

$$
p(x_t\mid x_1,\ldots,x_{t-1})
$$

这种训练方式通常称为 teacher forcing。

例如训练“我喜欢苹果”时，即使模型在前一个位置更倾向于预测“讨厌”，预测“苹果”时仍然输入真实上下文“我喜欢”，而不是模型自己刚才预测的“我讨厌”。

这样做的好处是：

- 每个位置都可以得到清晰的监督信号；
- Transformer 可以并行计算所有位置的损失；
- 训练更加稳定和高效。

它也会造成训练和生成之间的差异：

- 训练时看到的历史 token 来自真实数据；
- 生成时看到的历史 token 包含模型自己的预测。

模型早期生成错误后，后续预测可能继续受到这个错误影响。

## 10. 训练与生成不是同一件事

### 10.1 训练阶段

训练阶段使用最大似然：

$$
\min_\phi
-\sum_t\log p(x_t\mid x_{<t};\phi)
$$

真实 token 是已知的，目标是提高它的条件概率。

### 10.2 生成阶段

生成时真实的下一个 token 未知。模型先计算：

$$
p(x_t\mid \hat x_{<t};\hat\phi)
$$

然后使用某种解码策略选择下一个 token，例如：

- 贪心解码：选择概率最大的 token；
- 随机采样：按照预测分布采样；
- temperature：调整分布的尖锐程度；
- top-$k$：只在概率最高的 $k$ 个 token 中采样；
- top-$p$：在累计概率达到阈值的候选集合中采样；
- beam search：保留若干条高概率候选序列。

最大似然定义的是如何训练概率模型；解码策略定义的是如何使用这个概率模型生成文本。二者不能混为一谈。

## 11. 条件语言模型

聊天模型、翻译模型和摘要模型还会接收额外条件 $c$，例如用户问题或待翻译文本。

此时模型学习：

$$
p(x_{1:T}\mid c)
=
\prod_{t=1}^{T}p(x_t\mid c,x_{<t})
$$

负对数似然为：

$$
L(\phi)
=
-\sum_t
\log p(x_t\mid c,x_{<t};\phi)
$$

例如，在监督微调中：

- 条件 $c$ 可以是系统消息和用户问题；
- 输出 $x_{1:T}$ 是理想回答；
- 模型通过最大化理想回答在给定问题下的条件似然进行学习。

有些实现只对回答 token 计算损失，而把提示部分的标签设为忽略值。这样优化目标更集中于“给定提示后生成回答”。

## 12. 为什么不能说 token 相互独立

语言模型的似然中出现了乘积：

$$
p(x_{1:T})
=
\prod_t p(x_t\mid x_{<t})
$$

但这不表示 token 相互独立。每一项都依赖此前上下文。

可以区分两件事：

1. 一个序列内部通过条件概率保留 token 间依赖；
2. 训练时通常把不同文档或训练序列当作独立样本来组合数据集似然。

所以语言模型使用的是“条件分解”，而不是“把所有 token 当作无条件独立样本”。

## 13. 困惑度 Perplexity

不同文本长度不同，直接比较总负对数似然不公平。通常计算每个有效 token 的平均负对数似然：

$$
\overline L
=
-\frac{1}{M}
\sum_{m=1}^{M}\log p(x_m\mid x_{<m})
$$

其中 $M$ 是参与损失计算的 token 总数。

困惑度定义为：

$$
\operatorname{PPL}
=
\exp(\overline L)
$$

平均负对数似然越小，困惑度越低。直观上，困惑度可以粗略理解为模型在每一步面对的“有效候选数量”。

但比较困惑度时必须确保：

- 使用相同测试数据；
- 使用相同或可比的 tokenizer；
- 使用相同的 token 计数与掩码方法。

不同 tokenizer 下的 token 级困惑度通常不能直接比较。

## 14. 最大似然教会了语言模型什么

最大似然鼓励模型逼近训练数据的条件分布：

$$
p_{\text{model}}(x_t\mid x_{<t})
\approx
p_{\text{data}}(x_t\mid x_{<t})
$$

它能够学习：

- 语法规律；
- 词语搭配；
- 文本风格；
- 训练数据中呈现的事实和模式；
- 不同上下文下后续 token 的相对可能性。

但最大似然本身只要求模型模仿数据分布。它不会自动保证：

- 内容完全真实；
- 回答对用户最有帮助；
- 输出符合人类偏好；
- 生成过程没有重复；
- 模型不会学习训练数据中的偏差。

因此，现代语言模型还可能使用偏好优化、安全训练或其他目标。这些后续目标不一定都是标准 token 最大似然。

## 15. 最大似然的几个局限

### 15.1 暴露偏差

训练时使用真实历史，生成时使用模型自己的历史。两种输入分布不完全一致。

### 15.2 token 级目标与序列质量不完全一致

最大似然逐 token 奖励真实答案，但用户通常关心整个回答是否正确、连贯和有用。

### 15.3 多种合理回答的问题

对于开放式问题，训练集中通常只记录了一个回答，但实际上可能存在许多合理回答。最大似然会提高已记录回答的概率，却没有直接告诉模型其他合理回答也同样优秀。

### 15.4 数据分布决定学习方向

模型会学习训练语料中的常见模式，也可能学习其中的错误、噪声和偏差。

### 15.5 序列概率偏向短文本

序列概率是多个小于 1 的条件概率之积，文本越长，总概率通常越小。因此在搜索和比较候选序列时，常常需要长度归一化或其他修正。

## 16. 一段接近实际实现的伪代码

```python
# tokens: [batch_size, sequence_length]
inputs = tokens[:, :-1]
targets = tokens[:, 1:]

# logits: [batch_size, sequence_length - 1, vocabulary_size]
logits = model(inputs, causal_mask=True)

# cross_entropy 内部完成 log_softmax 和负对数似然计算
loss = cross_entropy(
    logits.reshape(-1, vocabulary_size),
    targets.reshape(-1),
    ignore_index=padding_token_id,
)

loss.backward()
optimizer.step()
```

实际实现通常直接把 logits 传给交叉熵函数，而不是先手动计算 Softmax。这样可以通过 `log_softmax` 等数值稳定算法避免上溢或下溢。

## 17. 用统一框架重新检查语言模型

语言模型完整地符合最大似然框架：

| 最大似然步骤 | 语言模型中的对应内容 |
|---|---|
| 确定输出空间 | 词表中的离散 token |
| 选择概率分布 | Categorical 分布 |
| 确定依赖结构 | $p(x_{1:T})=\prod_t p(x_t\mid x_{<t})$ |
| 参数化分布 | 神经网络输出 logits，Softmax 得到概率 |
| 写出似然 | 所有真实 token 条件概率的乘积 |
| 取负对数 | token 交叉熵之和或平均值 |
| 优化参数 | 反向传播和梯度优化 |
| 执行推理 | 根据条件分布逐 token 解码 |

## 18. 练习题

### 题目 1：序列概率

某模型对序列 `A B C` 给出：

$$
p(A)=0.5,
\qquad
p(B\mid A)=0.4,
\qquad
p(C\mid A,B)=0.2
$$

求整个序列的概率和负对数似然。

### 题目 2：判断独立性

下面的公式是否假设 token 相互独立？为什么？

$$
p(x_1,x_2,x_3)
=
p(x_1)p(x_2\mid x_1)p(x_3\mid x_1,x_2)
$$

### 题目 3：单位置交叉熵

词表为 `{猫, 狗, 鱼}`，真实下一个 token 是“狗”。模型预测概率为：

$$
(0.2,0.7,0.1)
$$

求该位置的负对数似然。

### 题目 4：平均损失与困惑度

一个序列三个位置的真实 token 概率分别为：

$$
0.5,\quad0.25,\quad0.5
$$

求平均负对数似然和困惑度。

### 题目 5：条件语言模型

在问答模型中，用户问题为 $c$，回答为 $x_{1:T}$。写出回答的条件概率分解和负对数似然。

### 题目 6：训练与生成

为什么训练时可以并行计算所有 token 的损失，而生成时通常需要逐 token 进行？

## 19. 参考答案

### 答案 1

序列概率为：

$$
p(A,B,C)
=
0.5\times0.4\times0.2
=
0.04
$$

负对数似然为：

$$
-\log(0.04)
\approx
3.219
$$

也可以分别计算三个位置的负对数概率再相加。

### 答案 2

没有假设 token 相互独立。第二项依赖 $x_1$，第三项依赖 $x_1,x_2$。这个公式是联合概率的链式法则，对任意联合分布都成立。

如果假设相互独立，公式才会变成：

$$
p(x_1,x_2,x_3)=p(x_1)p(x_2)p(x_3)
$$

### 答案 3

真实 token“狗”的概率是 $0.7$，所以：

$$
L=-\log(0.7)\approx0.357
$$

### 答案 4

总负对数似然为：

$$
L
=
-\log(0.5)-\log(0.25)-\log(0.5)
=
\log 8
$$

平均负对数似然为：

$$
\overline L
=
\frac{\log8}{3}
=
\log2
\approx0.693
$$

困惑度为：

$$
\operatorname{PPL}
=
\exp(\log2)
=
2
$$

### 答案 5

条件概率分解为：

$$
p(x_{1:T}\mid c)
=
\prod_{t=1}^{T}p(x_t\mid c,x_{<t})
$$

负对数似然为：

$$
L
=
-\sum_{t=1}^{T}\log p(x_t\mid c,x_{<t})
$$

### 答案 6

训练时完整序列已经已知。通过因果注意力掩码，每个位置都能同时读取各自允许看到的真实前缀，因此所有位置的 logits 可以并行计算。

生成时下一个 token 是未知的。生成第 $t+1$ 个 token 前，必须先得到第 $t$ 个 token 并把它加入上下文，所以通常需要按顺序迭代。

## 20. 最终心智模型

用一句话概括语言模型的最大似然训练：

> 把每个下一个 token 看作一个依赖历史上下文的 Categorical 随机变量，让神经网络预测它的概率分布，并通过最小化真实 token 的负对数概率来学习模型参数。

完整链条是：

$$
\boxed{
\begin{aligned}
&\text{序列联合概率}\\
&\downarrow\ \text{链式法则}\\
&\text{逐 token 条件概率}\\
&\downarrow\ \text{Categorical + Softmax}\\
&\text{真实 token 的概率}\\
&\downarrow\ \text{负对数}\\
&\text{交叉熵损失}\\
&\downarrow\ \text{梯度优化}\\
&\text{学习语言数据的条件分布}
\end{aligned}
}
$$

