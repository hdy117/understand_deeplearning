# 第 5 章：损失函数（Loss Functions）

> 书：《Understanding Deep Learning》Ch.5  
> 前置：第 3–4 章把网络写成一族由参数 \(\phi\) 决定的 piecewise linear 函数 \(f(x,\phi)\)。  
> 本章：怎样把“拟合得好不好”压成一个可最小化的标量？MSE、交叉熵这些常用损失从哪来？  
> 后续：第 6 章在参数空间里走；第 7 章算梯度；第 8 章衡量走得怎样。

---

## 0. 不要先背损失名字

常见问题是：“回归用 MSE，分类用交叉熵。”这句话当口诀能跑通作业，却把真正的选择藏起来了。网络先是一族函数，训练要在这族函数里挑一个。挑的依据必须是一个标量——否则梯度下降没有下山的方向。

这个标量从哪来？原书的答案不是菜单，而是一次视角切换：

> **网络预测的不是一个点 \(\hat y\)，而是给定 \(x\) 之后，输出 \(y\) 所服从分布的参数。损失是这个分布下、已观察数据的负对数似然。**

这两行不是两套互斥算法，而是**同一张网络、两种读法**。前向计算可以完全一样；差别在于：网络吐出的那个数，到底被当成答案本身，还是当成分布的参数。

传统点预测：

\[
\hat y = f(x,\phi).
\]

给定输入 \(x\) 和参数 \(\phi\)，网络吐出一个数（或一个向量），这个数**就是**对 \(y\) 的猜测。房价猜 \(310\) 万，到此结束。训练时拿 \(\hat y\) 和真实 \(y\) 比距离；模型默认不承认“我可能不确定”。

概率视角：

\[
\theta = f(x,\phi),
\qquad
y \sim P(y\mid\theta).
\]

同一张网吐出的不是答案本身，而是条件分布 \(P(y\mid x)\) 的参数 \(\theta\)。真正的 \(y\) 被看成从这个分布里来的。训练时并不真的抽样，只问：已经看见的那个 \(y\)，在这个分布里有多可能。\(\mu(x)\) 是“主要猜哪里”，\(\sigma^2(x)\) 是“这个位置有多不确定”——方差可以藏起来（处处相同），也可以让网络一起预测。

```text
x ──► 网络 f(·,φ) ──► θ ──► 分布 P(y|θ)
                              │
                              ▼
                         真实 y 落在哪、有多可能
```

### 同一套房：两种读法怎么算

输入 \(x\)：一套 80 平、地铁旁的房子。真实成交价 \(y=320\)（万元）。

**读法 A：点预测。** 网络输出 \(\hat y=f(x,\phi)=310\)。“我觉得这套房值 310 万。” 用平方误差量一量：

\[
(y-\hat y)^2=(320-310)^2=100.
\]

这就是一颗样本上的平方损失。\(310\) 旁边偏 10 万还是偏 100 万，对这套公式只是误差大小不同；模型自己没有“不确定”这个槽位。

**读法 B：把同一个 \(310\) 读成高斯均值。** 规定 \(\theta=\mu=310\)，并先假装全市场噪声一样大，\(\sigma^2=25\)（\(\sigma=5\) 万元）：

\[
y\sim\mathcal N(\mu,\sigma^2),\qquad
P(y\mid\mu,\sigma^2)
=\frac{1}{\sqrt{2\pi\sigma^2}}
\exp\Bigl(-\frac{(y-\mu)^2}{2\sigma^2}\Bigr).
\]

代入 \(y=320\)：

\[
\frac{(320-310)^2}{2\cdot 25}=2,
\qquad
P(320\mid 310,25)
=\frac{1}{\sqrt{50\pi}}e^{-2}
\approx 0.0178,
\qquad
-\log P\approx 4.03.
\]

平方误差 \(100\) 还在，只是被 \(2\sigma^2\) 除过，再加一个与 \(\mu\) 无关的 \(\log\sigma\) 项。**固定 \(\sigma\) 时，最小化 NLL 和最小化 \((y-\mu)^2\) 是同一件事。** 点预测里的 \(\hat y=310\)，在这里就是 \(\theta\) 里的 \(\mu\)。

**连方差一起预测。** 现实噪声随 \(x\) 变：学区房可能很准，郊区很飘。让网络输出 \(f(x,\phi)=\{\mu(x),\sigma^2(x)\}\)。

这里的**尺子**就是 \(\sigma\)：高斯把残差 \(y-\mu\) 按“几个标准差”来量，不按万元的绝对数来量。NLL 里真正进损失的那一项是

\[
\frac{(y-\mu)^2}{2\sigma^2}
=\frac12\left(\frac{y-\mu}{\sigma}\right)^2.
\]

分子是残差（万元），分母是尺子的刻度 \(\sigma\)。同一根残差，尺子短就显得偏得远，尺子长就显得没什么。

还是 \(y=320,\mu=310\)，残差 \(10\) 万元不变：

| 模型说的 \(\sigma\) | 残差用尺子量 | \(\dfrac{(y-\mu)^2}{2\sigma^2}\) | 模型在说什么 |
|---|---|---|---|
| \(5\)（前面那次） | \(10/5=2\sigma\) | \(100/(2\cdot 25)=2\) | 偏了两个标准差，相当离谱 |
| \(20\) | \(10/20=0.5\sigma\) | \(100/(2\cdot 400)=0.125\) | 只偏半格，本来就没那么确定 |

```text
σ = 5     |----|----|          10 万 = 两格，出格
          310      320

σ = 20    |--------------------|  10 万 = 半格，还在肚子里
          310                 320
```

点预测没有这根尺子，永远只看 \((y-\hat y)^2=100\)，学区房和郊区房同一刻度。概率视角允许网络把某些 \(x\) 的 \(\sigma\) 拉长：同样偏 10 万，不确定的区域罚得轻，确定的区域罚得重。后面说 MSE 是“处处同一根噪声尺子”，指的就是把 \(\sigma\) 钉死、不让它随 \(x\) 变。

分类是同一对公式。垃圾邮件 \(y=1\)，网络输出 \(0.9\)：点预测把它当标签再和 \(0.5\) 比阈值；概率读法把它当 Bernoulli 参数 \(\lambda=0.9\)，于是 \(P(y=1\mid\lambda)=0.9\)，\(-\log 0.9\approx 0.105\)。Sigmoid 的职责是保证 \(\lambda\in(0,1)\)，让 \(\theta\) 合法——不是另一种分类技巧。

| | 点预测 \(\hat y=f(x,\phi)\) | 概率 \(\theta=f(x,\phi),\; y\sim P(y\mid\theta)\) |
|---|---|---|
| 网络吐出的东西 | 答案 | 分布参数 |
| 房价例子 | \(\hat y=310\) | \(\mu=310\)（可再加 \(\sigma\)） |
| 垃圾邮件 | \(\hat y=0.9\) | \(\lambda=P(y=1)=0.9\) |
| 训练时比什么 | \(\hat y\) 和 \(y\) 的距离 | 真实 \(y\) 在 \(P\) 下的概率（NLL） |
| 不确定性 | 默认没有 | 写在 \(P\) 里 |
| 推理 | 直接用 \(\hat y\) | 可用均值、众数，或整条分布 |

一句话：\(f(x,\phi)\) 算的是同一个前向；你把它叫 \(\hat y\) 还是叫 \(\theta\)，决定了后面损失从哪来、推理能说什么。后面整章都在做同一件事：选定 \(P(y\mid\theta)\) 之后，NLL 会自动变成 MSE、BCE 或交叉熵。

调查链：

```text
函数族需要一个标量来说谁更好
        │
        ▼
把输出看成条件分布 P(y|x)，而不是一个点
        │
        ▼
最大似然：让已观察的 y 在模型里尽量可能
        │
        ▼
连乘会下溢、优化习惯最小化 → 负对数似然
        │
        ▼
选分布必须匹配 y 的定义域
        │
        ▼
Gaussian → MSE；Bernoulli → BCE；Categorical → 多类 CE
        │
        ▼
KL / 交叉熵是同一原则的另一张脸
```

一旦问的是“\(y\) 可能服从什么分布、我希望模型学它的哪个统计量”，输出层、激活、损失、推理方式通常会一起定下来。

---

## 1. 为什么必须有一个标量

神经网络代表一族函数。训练不是“让网络看起来聪明”，而是在这族函数里找一个对训练数据解释得最好的成员。损失函数 \(L(\phi)\) 把整份训练集上的预测质量压成一个数：

```text
整份 {(x_i, y_i)}
        │
        ▼
L(φ) 一个标量
        │
        ▼
第 6 章：沿 −∇L 更新 φ
```

损失越小，按这个标准，模型对训练数据的解释越好。它同时也把假设写进去了：噪声长什么样、错在哪里该罚多重、要不要表达不确定性。换损失，就是换“什么叫好”。没有这个标量，后面的梯度下降没有客观。

下一问：标量按什么原则构造？原书用最大似然，而不是“挑一个流行的公式”。

---

## 2. 最大似然：让看见的数据尽量可能

训练集记成 \(\{(x_i,y_i)\}_{i=1}^{I}\)，一共 \(I\) 对。网络对每个输入给出分布参数 \(\theta_i=f(x_i,\phi)\)。若模型合理，真实的 \(y_i\) 在这个分布下应该不太离谱。单条样本在模型眼里有多“像会发生”，写成

\[
P\bigl(y_i \mid f(x_i,\phi)\bigr).
\]

竖线左边是**已经看见的标签** \(y_i\)，右边是网络给出的分布参数。它不是“\(y_i\) 这个符号的概率”，而是：在当前 \(\phi\) 下，模型给这个已发生结果分配了多大的概率（离散）或密度（连续）。

先算一条分类。邮件 \(x_i\) 的真实标签 \(y_i=1\)（是垃圾），网络经 Sigmoid 给出 \(\lambda=f(x_i,\phi)=0.8\)，即 Bernoulli 参数。公式塌成真实类那一项：

\[
P(y_i=1\mid\lambda=0.8)=0.8.
\]

模型认为“这封确实是垃圾”有八成可能。若同一封信网络却给出 \(\lambda=0.1\)，则 \(P(y_i=1\mid 0.1)=0.1\)：真实结果在模型里变得很不像会发生。若真实标签是 \(y_i=0\)，同一 \(\lambda=0.8\) 给出的是另一头：\(P(y_i=0\mid 0.8)=1-0.8=0.2\)。竖线左边换了，数值就换了——公式永远只问**已经发生的那个 \(y_i\)**。

再算一条回归。房子 \(x_i\)，成交价 \(y_i=320\)，网络只预测均值 \(\mu=f(x_i,\phi)=310\)，固定 \(\sigma=5\)。这时 \(P\) 是高斯密度，不是 \(0\) 到 \(1\) 的概率：

\[
P(320\mid 310,\,25)
=\frac{1}{\sqrt{2\pi\cdot 25}}\exp\Bigl(-\frac{(320-310)^2}{2\cdot 25}\Bigr)
\approx 0.0178.
\]

\(0.0178\) 可以很小，甚至在 \(\sigma\) 更小时密度峰值可以大于 \(1\)；它比的是“这个 \(y\) 相对别的 \(y\) 有多密”，不是“百分之多少”。均值若改成 \(\mu=320\)，残差为 \(0\)，密度升到 \(1/\sqrt{50\pi}\approx 0.080\)，真实成交价在模型里更像会发生。分类的 \(0.8\) 和回归的 \(0.0178\) 单位不同，但都是同一条公式：当前 \(\phi\) 给这条已观察 \(y_i\) 打的分。

一条样本不够。整份数据 \(\mathcal D\) 的似然需要一个联合。假设各训练样本在给定各自输入后**条件独立**——知道 \(x_i\) 之后，\(y_i\) 不再依赖别的样本——联合就变成乘积：

\[
P(\mathcal D\mid\phi)
=
\prod_{i=1}^{I}
P\bigl(y_i \mid f(x_i,\phi)\bigr).
\]

\(\mathcal D\) 是整份训练集，\(\phi\) 是整张网的参数。乘积的含义是：每一条都要像会发生，有一条特别离谱（概率接近 0），整份似然就被拖到接近 0。独立是建模假设，不是数据事实；没有它，就不能把联合拆成每条 \(P(y_i\mid\theta_i)\) 再相乘。

最大似然（maximum likelihood）是在 \(\phi\) 里挑一个，让上面这个乘积尽量大：

\[
\hat\phi
=
\arg\max_\phi
\prod_{i=1}^{I}
P\bigl(y_i \mid f(x_i,\phi)\bigr).
\]

\(\arg\max_\phi\) 读成“使后面那个式子最大的那个 \(\phi\)”；头上的帽 \(\hat\phi\) 表示挑出来的那组参数，不是又一个网络输出。读成人话：参数应该让**已经发生的那些 \(y\)** 在模型里尽可能像“本就会发生的事”。还没发生的点，这一章先不谈——那是第 8 章的 generalization。

乘积在数值上会马上出事。许多小于 1 的数连乘，浮点很快下溢成 0，优化器看见一片平坦的零。对数把乘积变求和，又**严格递增**（\(a>b>0\) 则 \(\log a>\log b\)），最大值落在同一组 \(\phi\) 上：

\[
\log\prod_i P(y_i\mid f(x_i,\phi))
=
\sum_i \log P(y_i\mid f(x_i,\phi)).
\]

左边仍是整份数据的对数似然；右边是每条 \(\log P\) 相加。加法不会下溢，一条很差的样本贡献一个很大的负数，但不会把整个量砸成 \(0\)。因为 \(\log\) 不改变谁大谁小，\(\arg\max\) 对数似然和 \(\arg\max\) 原似然是同一组 \(\hat\phi\)。

优化习惯写成最小化（下山、梯度取负），于是再乘 \(-1\)，得到负对数似然（NLL）：

\[
\boxed{
L(\phi)
=
-\sum_{i=1}^{I}
\log P\bigl(y_i \mid f(x_i,\phi)\bigr).
}
\]

这就是本章要最小化的标量损失。负号只翻转方向：原来 \(\log P\) 越大越好（真实 \(y\) 越可能），现在 \(-\log P\) 越小越好。单条上看，模型给真实 \(y_i\) 的概率越接近 1，\(\log P\) 越接近 0，损失越接近 0；给得越接近 0，\(-\log P\) 越大，罚得越狠。求和表示整份训练集上把每条的罚加起来（有时再除以 \(I\)，只改尺度，不改最优 \(\phi\)）。

因此三件看起来不同的事是同一件事——同一组 \(\hat\phi\)，三种写法：

\[
\boxed{
\text{最大化数据似然}
\equiv
\text{最大化对数似然}
\equiv
\text{最小化负对数似然}.
}
\]

中间那步 \(\log\) 为了数值和求和；最后那步负号为了迁就“最小化损失”的惯例。没有改“什么叫拟合得好”。

### 一个小型手算：乘积、对数和负号

假设模型对三个已经观察到的标签给出的概率分别是 \(0.8, 0.5, 0.1\)。这三个数就是三条 \(P(y_i\mid f(x_i,\phi))\)。独立性假设下，整份数据的似然为乘积

\[
P(\mathcal D\mid\phi)=0.8\times0.5\times0.1=0.04.
\]

\(0.04\) 就是 \(\arg\max\) 里要拉大的那个数。直接最大化这个乘积当然可以，但对数似然更容易累加：

\[
\log P(\mathcal D\mid\phi)
=\log 0.8+\log 0.5+\log 0.1\approx-3.219,
\]

这就是 \(\sum_i\log P\)。所以这组样本的 NLL 是 \(L(\phi)=-\sum\log P=3.219\)。如果另一组参数把三个概率变成 \(0.9, 0.8, 0.5\)，似然为 \(0.36\)，NLL 只有 \(-\log 0.36\approx1.022\)，因此它更好：似然 \(0.36>0.04\)，对数似然 \(-1.022>-3.219\)，NLL \(1.022<3.219\)，三种比较选出同一组参数。负号只是把“越大越好”改成“越小越好”，没有改排序。

构造损失的统一配方因此极短：

```text
输出 y 的类型与定义域
        → 选择 P(y|θ)
        → 网络预测 θ = f(x,φ)
        → 最小化负对数似然
        → 推理时给出完整分布，或取众数 / 均值
```

下面不再“介绍三种常用损失”，而是三次把这个配方走完。每一次都先问 \(y\) 住在哪。

---

## 3. \(y\) 是无界实数：Gaussian 怎样变成 MSE

\(y\in\mathbb R\)。最省事的假设：给定 \(x\) 后，噪声是高斯，网络只预测均值，方差到处相同：

\[
y_i \sim \mathcal N\bigl(f(x_i,\phi),\,\sigma^2\bigr).
\]

把密度代入 \(-\log P\)，丢掉与 \(\phi\) 无关的常数，剩下

\[
L(\phi)
=
\sum_{i=1}^{I}
\bigl(y_i-f(x_i,\phi)\bigr)^2.
\]

均方误差不是从工程菜单里抽出来的。它隐含：

- 各样本条件独立；
- 误差是正态的；
- 所有 \(x\) 处方差相同（homoscedastic）；
- 网络学的是条件均值，不是中位数，也不是整个形状。

假设一破，MSE 就不必再是正确答案。误差有重尾、异常值多，Laplace / t 往往更合适，对应的 NLL 更接近绝对误差。输出其实是两个峰，均值可能落在两峰之间——那个位置数据里几乎从不出现。此时应预测多峰分布（高斯混合），而不是再平均一次。

### 手算一次：为什么平方误差出现

取两个样本，真实值和预测均值分别为

\[
\mathbf y=(3,1),\qquad \hat{\boldsymbol\mu}=(2,2).
\]

残差是 \((1,-1)\)，平方误差和为

\[
\sum_i(y_i-\hat\mu_i)^2=1^2+(-1)^2=2,
\]

因此未除以样本数的平方损失是 \(2\)，MSE 是 \(2/2=1\)。若固定方差为 \(\sigma^2=1\)，完整 Gaussian NLL 为

\[
\sum_i\left[\frac{(y_i-\hat\mu_i)^2}{2}+\frac12\log(2\pi)\right]
=1+\log(2\pi)\approx2.838.
\]

\(\log(2\pi)\) 与模型参数无关，所以训练时可以丢掉；除以样本数也只改变整体尺度，不改变最优参数。剩下的正是平方误差。

固定方差还留下一个缺口：现实噪声经常随 \(x\) 变。夜间的房价、边缘的深度、稀有类别附近的回归，不确定性并不处处一样。让网络同时输出

\[
f(x,\phi)=\{\mu(x),\sigma^2(x)\}
\]

就是 heteroscedastic regression：哪些区域比较确定、哪些区域噪声大，模型可以自己说。方差必须为正，所以原始 logit 通常要经平方、指数等正值变换，不能当无约束实数直接当 \(\sigma^2\)。

MSE 对应“处处同一根噪声尺子”。下一问换成 \(y\) 根本不是实数。

---

## 4. \(y\) 是 0/1：Bernoulli 怎样变成 BCE

二分类 \(y\in\{0,1\}\)。定义域已经不是 \(\mathbb R\)，Gaussian 没有座位。合适的是 Bernoulli：

\[
P(y\mid\lambda)=(1-\lambda)^{1-y}\lambda^y,
\]

\(\lambda\) 是 \(y=1\) 的概率，必须落在 \((0,1)\)。网络的原始输出却是任意实数，所以要先过 Sigmoid：

\[
\lambda=\operatorname{sigmoid}\bigl(f(x,\phi)\bigr).
\]

Sigmoid 不是点缀，它是在把无约束输出变成合法的分布参数。代入 NLL 得到二元交叉熵：

\[
L(\phi)
=
-\sum_i
\bigl[
y_i\log\lambda_i
+(1-y_i)\log(1-\lambda_i)
\bigr].
\]

正样本要把 \(\lambda\) 推高，负样本要把 \(\lambda\) 推低；错得又自信的预测，\(-\log\) 会罚得特别狠。这不是人为“加重惩罚”，是 Bernoulli 似然自己的形状。

### 手算一次：BCE 如何惩罚自信的错误

若真实标签为 \(y=1\)，模型预测 \(\lambda=0.8\)，则

\[
\ell=-\log 0.8\approx0.223.
\]

若真实标签为 \(y=0\)，模型预测 \(\lambda=0.2\)，数值同样是 \(0.223\)，因为两次都给真实结果 \(0.8\) 的概率。反过来，真实标签为 \(0\) 却预测 \(\lambda=0.8\) 时，

\[
\ell=-\log(1-0.8)=-\log 0.2\approx1.609.
\]

把“错且自信”从 \(0.8\) 推到 \(0.99\)，损失会变成 \(-\log 0.01\approx4.605\)。这就是 BCE 的曲线为何会强烈压低自信的错误。

两类不够时，\(y\) 变成一张 \(K\) 选 1 的标签。

---

## 5. \(y\) 是 \(K\) 类之一：Categorical 怎样变成多类 CE

\(y\in\{1,\ldots,K\}\)。分布是 categorical。网络给出 \(K\) 个无约束 logits \(z_1,\ldots,z_K\)，Softmax 把它们折成合法概率：

\[
\lambda_k
=
\frac{\exp(z_k)}{\sum_{k'=1}^{K}\exp(z_{k'})},
\qquad
\lambda_k\ge 0,
\quad
\sum_k\lambda_k=1.
\]

NLL 只看真实类那一项：

\[
L(\phi)
=
-\sum_i \log\lambda_{i,y_i}.
\]

这就是多类交叉熵。推理时常取 \(\hat y=\arg\max_k\lambda_k\)。和 Sigmoid 一样，Softmax 的职责是“合法化参数”，不是一种独立的分类技巧。

### 手算一次：Softmax 后只取真实类

设三个类别 \(A,B,C\) 的 logits 是 \((2,1,0)\)。先指数化并归一化：

\[
\operatorname{softmax}(2,1,0)
\approx(0.665,\,0.245,\,0.090).
\]

如果真实类别是 \(B\)，多类 CE 只读取 \(B\) 的概率：

\[
\ell=-\log 0.245\approx1.408.
\]

如果真实类别改成 \(A\)，损失则是 \(-\log 0.665\approx0.408\)。其余类别的概率仍会通过 Softmax 的归一化间接影响真实类概率，但 NLL 的显式项只有真实类那一项。

三次配方走完，可以看见一张不是用来背、而是用来对照定义域的表。

---

## 6. 选分布 = 匹配 \(y\) 住在哪

| 输出 | 常选的 \(P(y\mid\theta)\) | 常见后果 |
|------|---------------------------|----------|
| 无界连续实数 | 正态 | 普通回归、平方误差 |
| 含异常值的连续实数 | Laplace 或 t | 鲁棒回归、更接近绝对误差 |
| 多峰连续输出 | 高斯混合 | 多模态回归，避免均值落在两峰之间 |
| 正实数 | 指数或 Gamma | 时长、大小 |
| \([0,1]\) 区间 | Beta | 比例 |
| 多维连续 | 多元正态 | 多变量回归 |
| 圆周方向 | von Mises | 角度 |
| 二值 | Bernoulli | 二分类、BCE |
| 多类别 | Categorical | 多分类、CE |
| 非负整数 | Poisson | 计数 |
| 排列 | Plackett-Luce | 排序 |

原则只有一句：概率分布必须与输出的定义域和数据性质匹配。流行损失只是某些匹配碰巧常用。

若目标 \(\mathbf y\) 有多个分量，常见简化是：给定 \(x\) 后各输出条件独立，

\[
P(\mathbf y\mid f(x,\phi))
=
\prod_d P(y_d\mid f_d(x,\phi)).
\]

NLL 变成求和，于是风向用 von Mises、风速用正数分布，两个负对数似然可以直接加。独立是假设，不是事实；分量之间若有必须保留的相关，就要写联合分布。

到这里，损失已经能从“\(y\) 是什么”推出来。还差一张脸：同一原则也可以说成“让模型分布靠近数据分布”。

---

## 7. 交叉熵、KL、NLL 是同一件事的三张脸

把训练标签看成经验分布 \(q(y)\)，模型给出 \(p(y\mid\theta)\)。KL 散度

\[
D_{\mathrm{KL}}(q\Vert p)
=
\int q(y)\log q(y)\,dy
-
\int q(y)\log p(y\mid\theta)\,dy
\]

衡量“用 \(p\) 来编码 \(q\) 要多付多少额外信息”。第一项只由数据决定，与 \(\phi\) 无关。最小化 KL 因此等价于最小化第二项，也就是交叉熵。当 \(q\) 由训练样本构成，交叉熵塌成

\[
-\sum_i \log P\bigl(y_i\mid f(x_i,\phi)\bigr),
\]

正是 NLL。所以：

\[
\boxed{
\text{最大似然}
\equiv
\text{最小化负对数似然}
\equiv
\text{最小化经验分布与模型分布的交叉熵}.
}
\]

### 离散例子：KL 为什么只差一个与模型无关的常数

令数据分布 \(q=(0.5,0.5)\)，模型分布 \(p=(0.8,0.2)\)。交叉熵为

\[
H(q,p)=-0.5\log 0.8-0.5\log 0.2\approx0.916.
\]

数据自身的熵是

\[
H(q)=-0.5\log 0.5-0.5\log 0.5=\log 2\approx0.693.
\]

因此

\[
D_{\mathrm{KL}}(q\Vert p)=H(q,p)-H(q)\approx0.916-0.693=0.223.
\]

只要 \(q\) 固定，\(H(q)\) 就是常数；改变模型 \(p\) 时，最小化 KL 和最小化交叉熵会选出完全相同的参数。

三张脸各照亮一件事：似然说“提高已观察数据的概率”；NLL 说“给我一个可最小化的标量”；KL / 交叉熵说“让模型分布去贴数据分布”。第 17 章的 ELBO 会再碰到 KL，那时它出现在 latent 空间里，原则不变。

---

## 8. 几件选损失时立刻会踩的事

采用一个损失，就是对输出分布表态。MSE 常用，不等于它适合所有回归。

模型可以预测不确定性。点预测把 \(\sigma\) 藏起来；风险敏感任务往往需要方差、分位数或整条分布。

输出层必须和损失配套。Sigmoid / Softmax 负责把无约束网络输出变成合法参数，再交给相应的 NLL。

损失决定模型学哪个统计量：Gaussian+MSE 学条件均值；Laplace+绝对误差学条件中位数；categorical+CE 学类别条件概率。问错统计量，再大的网络也在拟合一个数据里不存在的点。

---

## 9. 自测

1. 点预测 \(\hat y=f(x,\phi)\) 和 \(\theta=f(x,\phi),\,y\sim P(y\mid\theta)\) 差在哪？  
2. 为什么似然要取对数再取负，最大值的位置却不变？  
3. 写出从输出类型到 NLL 的四步配方。  
4. 固定方差的 Gaussian 怎样导出 MSE？它偷偷假设了哪四件事？  
5. 什么时候应该让网络同时预测 \(\sigma^2(x)\)？为什么方差不能是任意实数？  
6. 二分类为什么需要 Sigmoid？它在配方里扮演哪一步？  
7. Softmax 保证了概率的哪两条性质？多类 CE 为什么只取真实类那一项？  
8. 输出多峰时，只学均值会怎样？  
9. 多输出任务里，把 NLL 相加用了什么独立性假设？  
10. 为什么最小化 \(D_{\mathrm{KL}}(q\Vert p)\) 与最小化 NLL 是同一件事？  
11. “这个任务通常用哪个损失”和“\(y\) 服从什么分布”哪一句才是母问题？

---

## 10. 合上书再看一眼

损失函数把整份训练数据压成一个标量，好让第 6 章有地方下山。构造这个标量的原则是最大似然：网络预测 \(P(y\mid x)\) 的参数，已观察的 \(y\) 应该在这个分布里尽量可能。对数和负号只是把乘积变成稳定的最小化。

于是 MSE、BCE、多类 CE 不再是三个名字，而是三种定义域上的 NLL。Gaussian 给出平方误差，Bernoulli 加 Sigmoid 给出 BCE，Categorical 加 Softmax 给出交叉熵。换定义域，就该换分布；换分布，统计量、输出层和推理方式一起换。

下一章假定这个标量已经在手，问的是：在参数空间里，怎样一步步把它变小。
