# 第 15 章：生成对抗网络（Generative Adversarial Networks）

> 书：《Understanding Deep Learning》Ch.15  
> 前置：第 14 章把 GAN 放在「会生成、通常不写 \(p(x)\)」那一格，用锐度和速度换精确打分。  
> 本章：没有 likelihood 时，训练信号从哪来？判别器太强为什么会把梯度掐死？  
> 后续：第 16 章把可逆性买回来，换精确的 \(p(x)\)。

---

## 0. 判别本身可以当监督信号

第 14 章已经把生成写成：从简单噪声 \(z\) 走到样本 \(x\)。GAN 的 generator \(g(z,\theta)\) 就是这个映射。缺的是第 5 章那种 NLL：没有归一化的 \(p_\theta(x)\)，就没有「这个点有多可能」的标量。

原书的替代原则很蛮：

> **若第二张网——discriminator——分不清样本是真的还是造的，生成器就赢了。**

Discriminator \(f(x,\phi)\) 输出「像真的」的程度。它被训成区分真实例子和生成样本；generator 被训成让判别器更犹豫。交替更新，两边互相拆台。最后判别器大约只有五成把握，被丢掉，留下一个只会从 \(z\) 造 \(x\) 的生成器。

这句话简单，训练却臭名昭著：不稳定，样本可以很像，却只覆盖数据的一小块（mode collapse）。整章只追这个缺口：

> **「分不清」怎样变成可反传的损失？这个信号在什么时候会断掉，以及后面那些架构和花招各自补的是哪一截？**

调查链：

```text
没有 p(x)，用「骗过判别器」当损失
        │
        ▼
minimax：D 要分清，G 要让 D 分不清
        │
        ▼
D 太强、两个支撑几乎不相交 → 梯度消失
        │
        ▼
Wasserstein：不相交也能量「搬动质量」的距离
        │
        ▼
工程：渐进长大、batch 里看多样性、截断 z
        │
        ▼
条件向量 c：指定要造哪一类
        │
        ▼
图像翻译：丢掉 z，判别器变成「像不像」的先验
        │
        ▼
StyleGAN：风格和噪声按尺度分别注入
        │
        ▼
仍不会打分 → 第 16 章用可逆变换写 p(x)
```

---

## 1. 损失：二分类交叉熵被拆成两人博弈

判别器是二分类：真例标签 \(1\)，生成样本标签 \(0\)。第 5 章的 BCE 变成

\[
\hat\phi
=
\arg\min_\phi
\left[
\sum_j -\log\bigl(1-\sigma(f(g(z_j,\theta),\phi))\bigr)
-
\sum_i \log\sigma(f(x_i,\phi))
\right],
\]

其中 \(\sigma\) 是 logistic sigmoid。Generator 的目标相反：希望生成样本被判成真，于是对 \(\theta\) 最大化同一堆东西。合在一起是 minimax：

\[
\hat\theta
=
\arg\max_\theta
\min_\phi
L(\phi,\theta).
\]

实践中拆成两个最小化：

\[
\begin{aligned}
L[\phi]
&=
\sum_j -\log\bigl(1-\sigma(f(g(z_j,\theta),\phi))\bigr)
-
\sum_i \log\sigma(f(x_i,\phi)),\\
L[\theta]
&=
\sum_j \log\bigl(1-\sigma(f(g(z_j,\theta),\phi))\bigr).
\end{aligned}
\]

第二式丢掉了不含 \(\theta\) 的真例项，再乘 \(-1\) 改成最小化。一步训练：从 base density 抽一批 \(z_j\) 得到 \(x^*_j=g(z_j,\theta)\)，再抽一批真例 \(x_i\)，对两个损失各自做一轮或几轮梯度下降。

把 sigmoid 输出当成已经算完的“像真的概率”，BCE 就是第 5 章那两行。一个真例、一个假例。判别器给出

\[
\sigma(f(x_{\mathrm{real}}))=0.9,
\qquad
\sigma(f(x_{\mathrm{fake}}))=0.1.
\]

它对真例很有把握、对假例也很有把握。D 的两项：

\[
-\log 0.9 \approx 0.105,
\qquad
-\log(1-0.1)=-\log 0.9\approx 0.105,
\qquad
L_D\approx 0.21.
\]

G 只看假例。非饱和写法希望假样本被判成真，用 \(-\log\sigma(f(x_{\mathrm{fake}}))=-\log 0.1\approx 2.30\)。饱和写法（minimax 里那项）是 \(\log(1-\sigma)=\log 0.9\approx -0.105\)，对 \(\theta\) 最小化等价于把 \(1-\sigma\) 再压小——但 \(\sigma=0.1\) 已经很小，斜率 \(\propto 1/(1-\sigma)\) 也小。**同一对数字：D 很轻松（0.21），G 在饱和损失下几乎没梯度，在 \(-\log D\) 下被狠狠推一把（2.30）。** 实践改用非饱和损失，不是换了一个哲学，是躲开 \(\log(1-D)\) 在 \(D\approx 0\) 时的平地。

若判别器反过来糊涂：\(\sigma_{\mathrm{real}}=0.5,\sigma_{\mathrm{fake}}=0.5\)，则 \(L_D=2\log 2\approx 1.39\)，G 的 \(-\log 0.5\approx 0.69\)。D 没提供“往哪边更像真”的方向，G 拿到的是噪声级信号。训练要的是夹在 \(0.9/0.1\) 和 \(0.5/0.5\) 之间的窄带。

**Deep convolutional GAN (DCGAN)** 把这套手续接到图像上：\(z\) 常是 100 维，generator 用转置卷积一路放大成图；discriminator 是普通卷积网，最后一层给出真假分数。训完丢掉 D，采样只走 \(z\sim p(z)\)、\(x=g(z,\theta)\)。

理论上到这里就结束了。要让 DCGAN 真的能训，原书列了当时就必须加上的零件：批归一化（generator 除末层、discriminator 除首层）、判别器里的 leaky ReLU、Adam。即便如此，换一套数据或换 MLP 生成器，就容易得到低质量、彼此雷同的图。

交替更新的时序不要写成“同时对一个 saddle 求导”。一步里 \(\theta\) 当常数训 \(\phi\)，下一步 \(\phi\) 当常数训 \(\theta\)。两边的学习率、每边更新几次，全是在调上一节那个窄带：D 领先太多，假样本的 \(\sigma\) 掉到 \(0.1\) 以下，饱和损失的坡没了。

---

## 2. 信号为什么会断：最优判别器与消失的梯度

把 \(L[\phi]\) 写成期望，最优判别器对来路不明的 \(\tilde x\) 给出

\[
\sigma\bigl(f(\tilde x,\phi)\bigr)
=
\frac{\Pr(\tilde x)}{\Pr(\tilde x)+\Pr(x^*)},
\]

也就是「真分布」和「生成分布」在该点的相对高度。代回损失，目标变成两种分布之间的一种散度（Jensen–Shannon 的亲戚）。分布完全分开时，这个距离已经饱和：再稍微挪一下生成样本，损失不动。

这不是病理特例。生成样本活在 \(z\) 维那么薄的一张流形上；真数据也因为物理过程而活在低维结构里（第 1 章那 42 块脸部肌肉）。两张薄片在高维里几乎不相交，判别器可以近乎完美切开，generator 拿到的梯度接近 0。

实验对应也很直：冻住 DCGAN 的 G，把 D 多训几个 epoch，G 的梯度迅速掉下去（对数坐标）。**判别器和生成器必须处在很窄的能力平衡里**：D 太差，信号是噪声；D 太好，信号消失。Mode collapse 是另一头的病：G 找到少数能稳定骗过 D 的点，然后不再理 \(z\)，多样性崩掉。第 14 章的 fidelity vs coverage，在这里变成训练动力学。

支撑不相交时，最优 D 可以处处给出确定性标签。真分布活在 \(\{0\}\)，生成分布活在 \(\{10\}\)：两个点质量，中间没有重叠。最优判别器

\[
\sigma(f(0))=1,\qquad \sigma(f(10))=0.
\]

JS 散度（对称化的 KL）在两个分布不交时取到上限 \(\log 2\)。把假质量从 \(10\) 挪到 \(9\)：支撑仍然不交，最优 D 仍是“真点给 1、假点给 0”，JS 仍是 \(\log 2\)。**搬了 1 个单位路程，散度一个台阶都不下。** 这就是饱和：距离已经封顶，局部挪动 squashing 成零梯度。GAN 的 BCE 目标在最优 D 附近逼近这类散度，所以“D 太强 + 两团分开”不是调参运气差，是尺子自己封顶了。

第 14 章那三个 mode 只打中一个，在这里同样读得通：G 把质量堆在一个能稳定骗 D 的点上，支撑更不相交，D 更容易给出 0/1，梯度更干。Mode collapse 和梯度消失会互相喂。

---

## 3. Wasserstein：不相交时仍然能量距离

换一把即使支撑不相交也平滑的尺子。**Wasserstein / earth mover’s distance** 问：把一堆概率质量搬成另一堆，最小要做多少功（质量 \(\times\) 路程）。离散情况是运输计划 \(P_{ij}\) 的线性规划；连续情况的对偶形式更适合网络：

\[
D_w\bigl(\Pr(x),q(x)\bigr)
=
\max_{f}
\left(
\int \Pr(x)f(x)\,dx
-
\int q(x)f(x)\,dx
\right),
\]

约束是 \(f\) 的 Lipschitz 常数小于 1（梯度绝对值处处 \(<1\)）。神经网络用 \(f(x,\phi)\) 去逼近这个「1-Lipschitz 函数族里的最大者」，积分用样本近似，得到 **Wasserstein GAN** 的 critic 损失：

\[
L[\phi]
=
\sum_j f\bigl(g(z_j,\theta),\phi\bigr)
-
\sum_i f(x_i,\phi).
\]

Generator 最小化 \(-\sum_j f(g(z_j,\theta),\phi)\)：让 critic 觉得假样本更「真」。和 BCE 版的关键差别：两个分布哪怕完全分开，把假样本往真样本搬近一点，Wasserstein 距离就会降一点，梯度不断。

代价是 Lipschitz 约束必须被真正守住。最初的做法是 clip 判别器权重；后来改成 **gradient penalty**（惩罚 \(\|\nabla_x f\|\) 偏离 1）或 **spectral normalization**（用权重矩阵的谱范数卡住每层的 Lipschitz）。这些不是装饰，是对偶形式能成立的条件。

同一对点质量，Wasserstein-1 有闭式。一维里它就是两个 CDF 之间的面积；两个 Dirac 之间就是它们的距离：

\[
W_1\bigl(\delta_0,\delta_{10}\bigr)=|10-0|=10.
\]

假质量挪到 \(9\)：\(W_1=9\)。挪到 \(1\)：\(W_1=1\)。**每靠近 1，距离就少 1，斜率恒为 1。** JS 在这一整段路上都钉死在 \(\log 2\)；earth mover 一直在报“还差多远”。1-Lipschitz 的最优 critic 就是一条斜率为 \(\pm 1\) 的直线（或夹在两端之间的斜坡）：从真点到假点，高度差正好等于路程。没有 Lipschitz，critic 可以把两端的函数值差拉成任意大，对偶爆炸；clip / GP / SN 守的就是这句话。

把第 14 章的 coverage 再接回来：Wasserstein 仍可能允许 G 把所有质量堆到最近的一个真 mode 上——那是最短搬运——所以换尺子治梯度，不自动治 mode collapse。Minibatch discrimination 补的仍是另一截。

---

## 4. 三件提高图像质量的工程切口

损失换了仍不够。原书把三件经常一起出现的技巧收在同一节，因为它们补的是三件不同的事。

**Progressive growing。** 先训一个造 \(4\times 4\) 的 GAN，再给 G 和 D 同时加一层，淡入到 \(8\times 8\)，再淡入更大。高分辨率层一开始只是上采样加残差，新层逐渐接管。先学会「一张脸的布局」，再学毛孔，比一开始就在 \(1024^2\) 上博弈稳定。

**Minibatch discrimination。** 在判别器靠后的层，计算 batch 内的特征统计，当成额外特征。D 因此能看见「这一批够不够多样」，并把这个信号传回 G。它针对的是 mode collapse，不是清晰度。

**Truncation。** 采样时丢掉离均值太远的 \(z\)（超过 \(\tau\) 个标准差）。一维标准正态，\(\tau=2\) 大约丢掉两侧各 \(2.3\%\) 的尾巴；\(\tau=0.7\) 几乎只留中心鼓包。\(\tau\) 大：多样、偶有缺陷；\(\tau\) 小：更干净、更像同一张脸。第 14 章 fidelity vs coverage 的旋钮，在 GAN 里被拧在 prior 的尾巴上。

再加上正交正则、历史假样本缓冲、给 D 的输入加噪、label smoothing，GAN 才能在图上走出「看起来像」这一格。在 latent 里平滑走动，有时还能得到看起来合法的插值——这是第 1 章那张「内部坐标」的许诺，在 GAN 里偶尔兑现，从不保证。

---

## 5. 条件生成：把「造谁」写进输入

无条件 GAN 不能指定发色或类别，除非为每种组合各训一个。**Conditional GAN** 把属性向量 \(c\) 同时喂给 G 和 D：\(g(z,c,\theta)\)、\(f(x,c,\phi)\)。G 要造出带正确属性的图；D 要区分「假图+目标属性」和「真图+真属性」。\(c\) 可以拼在 \(z\) 后面；对图像，常常线性变成一张图，当作额外通道接进去。

**ACGAN** 更省事：D 除了真假，还要预测类别。离散属性 \(C\) 类时，D 有 \(C+1\) 个输出：一个 sigmoid 管真假，其余 softmax 管类别。G 必须既像真的、又像被点名的那一类。

**InfoGAN** 不预先规定属性。G 吃噪声 \(z\) 和一组随机属性 \(c\)；D 既判真假，又去估计 \(c\)。直觉是：真实世界里可解释的因素最好预测，于是会自己占据 \(c\)。离散 \(c\) 用交叉熵，连续 \(c\) 用平方损失。它找到的是「好预测的因子」，不是保证语义解耦。

---

## 6. 图像翻译：判别器变成「像不像」的先验

对抗判别器不一定用来从噪声造图。它可以当作一项损失：鼓励输出看起来像目标域的真图。这时随机 \(z\) 往往被丢掉，留下的是一张图到另一张图的映射。

**Pix2Pix** 用 U-Net 做 \(x=g(c,\theta)\)，例如灰度 \(\rightarrow\) 彩色。内容损失 \(\|x-g(c,\theta)\|_1\) 要求贴住配对的真输出；对抗损失要求「像一张真彩图」。没有对抗项，\(\ell_1\) 容易给出模糊的条件均值（第 5 章：Laplace/L1 学的是条件中位数，均值回归会糊）。判别器在这里补的是第 14 章的 fidelity，不是 coverage。

**CycleGAN** 处理没有成对数据的情况。两个生成器 \(G:A\to B\)、\(F:B\to A\)，两个判别器。除了两边的对抗损失，再加 cycle consistency：\(F(G(a))\approx a\)、\(G(F(b))\approx b\)。它假设「风格可以改、内容应能走一圈回来」。这个假设错了就会改内容，不是免费午餐。

---

## 7. StyleGAN：按尺度分开风格和噪声

到目前为止，\(z\) 只从网络最底端进去。StyleGAN 选择在不同深度、以不同方式注入两类 latent，从而把数据集里的变化拆开。对人脸：大尺度是脸型和姿态，中尺度是五官形状，细尺度是发色肤色；**style** 是对人显著的因素，**noise** 是发丝、胡茬、雀斑、毛孔这种不该被 \(z\) 死死钉住的变化。

主干不从 \(z\) 起步，而从一个学出来的 \(4\times 4\times 512\) 常数，卷积一路上采样。每一尺度注入：

- style：由 \(z\) 经映射网络得到的 \(w\)，去调制该层的特征统计（AdaIN 一类）；越靠近输出，管的尺度越细；
- noise：独立的高斯图，加在卷积之后，负责「这一根头发精确长在哪」。

结果是：拧某一段 style，姿态或五官会变；加大噪声幅度，皮肤纹理变，身份不太动。第 14 章警告过 latent 不一定可拧；StyleGAN 用架构把「可拧」硬做进去，不是 GAN 训练自动长出来的属性。

---

## 8. 自测

1. GAN 的生成模型里有哪些分布？训练结束后还留下谁？  
2. 为什么 BCE 对 \(\theta\) 是最大化、对 \(\phi\) 是最小化？拆成两个 \(L\) 时，哪一项被丢掉了？  
3. D 达到最优时，\(\sigma(f(\tilde x))\) 等于哪两个密度的比？这件事怎样连到 JS 散度？  
4. 真假支撑几乎不相交时，为什么 generator 梯度会消失？这和第 1 章「观测维数虚高」有什么关系？  
5. Mode collapse 伤的是 fidelity 还是 coverage？Minibatch discrimination 针对哪一个？  
6. Wasserstein 距离在支撑不相交时为什么仍提供斜率？对偶形式对 \(f\) 提出了什么约束？  
7. Weight clipping、gradient penalty、spectral normalization 各自在守哪句话？  
8. Progressive growing 先学什么、后学什么？Truncation 的 \(\tau\) 在保真和多样之间怎样换？  
9. cGAN 和 ACGAN 把属性分别交给谁？InfoGAN 的 \(c\) 为什么「好预测」不等于「语义解耦」？  
10. Pix2Pix 为什么不能只靠 \(\ell_1\)？CycleGAN 的 cycle consistency 写进了哪条未必为真的假设？  
11. StyleGAN 为什么不从 \(z\) 直接开画，而从一个学出来的常数图起步？style 和 noise 各管什么？  
12. 第 14 章说 GAN 用什么换什么？训完之后，你怎样给一张新图打 \(p(x)\)？  
13. \(\sigma_{\mathrm{real}}=0.9,\sigma_{\mathrm{fake}}=0.1\) 时，\(L_D\) 两项各是多少？饱和 G 损失与 \(-\log D\) 差在哪？  
14. 两个 Dirac 在 \(0\) 与 \(10\)：最优 D、JS、把假点挪到 \(9\) 之后 JS 变了吗？\(W_1\) 呢？

---

## 9. 合上书再看一眼

GAN 不写 \(p(x)\)。它把「像不像」外包给一张判别网：D 要分清，G 要让 D 分不清。\(\sigma=0.9/0.1\) 时 D 很轻松、饱和 G 损失几乎没坡；支撑钉在 \(0\) 与 \(10\) 时 JS 封顶在 \(\log 2\)，假点挪到 \(9\) 也不动；同一对点的 \(W_1\) 从 \(10\) 降到 \(9\)，斜率还在。Mode collapse、渐进长大、截断 \(z\)、条件向量、翻译任务里的对抗先验、StyleGAN 按尺度注入，全部是在同一条轴上补不同的缺口：有的补梯度，有的补覆盖，有的补可控性。

判别器在采样时不在场。留下的只有 \(z\mapsto x\)。下一章把这个映射改成必须可逆，于是同一套网络既能造样本，又能给样本写出精确的密度。
