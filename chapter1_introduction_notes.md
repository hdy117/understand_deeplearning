# 第 1 章：导论（Introduction）

> 书：《Understanding Deep Learning》Ch.1  
> 前置：无。这是全书的入口。  
> 本章：机器学习被粗分成哪三问？深度网络为什么会同时出现在这三问里？伦理为什么不能当附录？  
> 后续：第 2 章把 supervised 从黑箱拆成「函数族 + 损失 + 训练」。

---

## 0. 先分清三层词，再分清三问

日常对话里 AI、machine learning、deep learning 经常被当成同一个东西。原书把它们叠成包含关系，而不是三个时髦同义词：

```text
AI：做「像智能」的系统（逻辑、搜索、概率推理都算）
        │
        ▼
Machine learning：用观测数据拟合数学模型，再拿模型做决定
        │
        ▼
Deep learning：模型具体选成 deep neural network，拟合过程叫训练
```

本书几乎不写证明，也几乎不写工程代码。它要的是：你换一个没有现成菜谱的新问题，仍然知道该先问什么。

机器学习本身又被粗分成三块：**supervised**、**unsupervised**、**reinforcement learning**。当代最强的方法在三块里都用上了深度网络。容易一上来就黏住的误会是：把这三块读成「从易到难」或「从有标签到没标签」的梯子。它们其实是三个不同的母问题：

| 问题 | 数据里有什么 | 模型被要求做什么 |
|------|----------------|------------------|
| Supervised | 成对的 \((x,y)\) | 学一个从输入到输出的映射 |
| Unsupervised | 只有 \(\{x_i\}\) | 描述或生成数据本身的结构 |
| RL | 动作会改变下一时刻的状态 | 选动作，让长期回报尽量高 |

三问用同一批「小数字」就能分开，不必先上大模型。

**Supervised。** 四套房子：面积（平）对成交价（万元）是 \((50,180),\ (80,310),\ (80,330),\ (120,450)\)。老师已经把 \(y\) 写在纸上。模型要学 \(x\mapsto y\)；同一面积 \(80\) 对应两个价，说明映射允许噪声，但问题仍然是「给定 \(x\) 猜 \(y\)」。训完之后来一套没见过的 \(90\) 平，吐出一个价，这一步叫 inference。

**Unsupervised。** 把价格撕掉，只剩面积 \(\{50,80,80,120\}\)。没有老师可 supervises。能问的变成：这些数挤在哪一段？能不能再造一个「看起来也像这批房子」的面积，比如 \(95\)，而不是乱抽一个 \(8\) 或 \(9000\)。生成模型走得更远：连像素一起造一张「像训练集」的新图。

**RL。** 机器人每步选「前进 / 左转」，到达检查点得 \(+10\)，摔倒 \(-1\)。走了 20 步才第一次拿到 \(+10\)：那 \(+10\) 该记在第 3 步的左转，还是第 19 步的前进？数据本身还取决于它刚才选了什么——继续走已经会的路线，平均每局 \(+4\)；试没试过的爬坡，可能到 \(+12\)，也可能先连续摔成 \(-8\)。这不是缺标签的 supervised，是动作在改下一时刻从哪来。

调查链：

```text
ML 是用数据拟合模型，不是魔法
        │
        ▼
Supervised：标签对当老师，模型是一族函数
        │
        ▼
输入/输出都可以带内部结构，黑箱方程必须吃得下
        │
        ▼
Unsupervised：没有 y，本书聚焦「再造一份像训练数据的新样本」
        │
        ▼
观测维数常常虚高 → 用更简单的 latent 当内部坐标
        │
        ▼
RL：动作不但决定现在，还决定下一份数据从哪来
        │
        ▼
这套工具会改世界 → ethics 不是附录
        │
        ▼
全书按「先会训 → 再往结构里写偏好 → 再会生成 → 再问为什么能成」走
```

---

## 1. Supervised：标签对当老师

Supervised learning 要的是一个映射：输入进来，输出一个预测。房子特征 \(\rightarrow\) 房价，评论文本 \(\rightarrow\) 正/负，音频片段 \(\rightarrow\) 音乐类型，图像 \(\rightarrow\) 物体类别。现在先把模型当成黑箱：一边吃数字向量，一边吐数字向量。真正的方程留到第 2–4 章。

### 1.1 输出是连续数，还是类别？

回归返回连续量。房价是一个数；分子同时预测熔点和沸点，就是 multivariate regression。分类把输入分到类别里：二分类返回两类的概率，多类返回长度为 \(N\) 的概率向量。

这不是两套宇宙。第 5 章会把它们收成同一句话：网络预测的是 \(P(y\mid x)\) 的参数，损失是负对数似然。现在只需记住：输出的类型会决定后面损失长什么样。

### 1.2 输入并不都是一袋数字

表格数据（面积、卧室数）没有内部次序：打乱特征再训一个新模型，预测不该变。文本就不是：`my wife ate the chicken` 和 `the chicken ate my wife` 不是同一件事，长度还会变。十秒 44.1 kHz 音频大约 441,000 个整数；图像是每个像素的 RGB 拼起来，上下相邻的像素在向量里甚至不相邻。分子更麻烦：原子数量不固定，连接方式才是数据。

所以「一个方程」必须能处理：很大、变长、带空间或图结构的输入。深度网络被选中，首先是因为它代表的函数族足够宽，而且相对容易在这个族里搜索。

### 1.3 模型是一族曲线，训练是挑其中一条

小孩身高对年龄：模型不是「那一条青线」，而是所有可能青线组成的家族。训练数据是橙点，比如 \((6,115),\ (8,128),\ (10,139),\ (12,151)\)（岁，cm）。**训练**的意思是：在这个家族里找一条最能描述这些点的曲线。把 \(10\) 岁代入已经选定的方程得到 \(139\) cm，这一步叫 **inference**。同一组点可以配很多条线；「学会了」指的是参数被数据钉住，不是方程形式自己长出来。

那些成对样本扮演老师，所以叫 supervised。音乐分类需要人标过体裁的音频；没有这些对，这一章的问题根本提不起来。

### 1.4 输出也可以有语法

语义分割给每个像素贴「牛 / 背景」；单目深度估计给每个像素一个深度。这两类输出虽然高维，但结构和输入绑在一起：邻居像素颜色相近，标签也往往相近。

更难的是输出的语法不跟输入走：语音转写、英译法、根据 caption 画图。这里有两件事同时发生。第一，正确答案往往不止一个——一句英文有多种合法法译，一个 caption 对应许多张图。第二，不是任意词串都是句子，不是任意 RGB 堆都是图。除了学映射，还得尊重输出自己的「语法」。

语法可以在没有输出标签时学：大量无标注文本就能告诉你什么叫合法英文。这句话把 supervised 的缺口递给了下一节。

---

## 2. Unsupervised：拿走 \(y\) 之后还在学什么

没有对应的输出标签，就叫 unsupervised：没有老师可 supervises。目标不再是 \(x\mapsto y\)，而是描述数据本身的结构。数据仍然可以是离散或连续、低维或高维、定长或变长。

原书后半本把镜头对准 **generative unsupervised models**：学着合成与训练数据在统计上无法区分的新样本。有的模型会写出 \(p(x)\)，新样本是从这个分布里抽的；有的只学会「造样本」的机制，并不显式给概率。第 14 章会把「会生成」和「会打分」拆成两件不同的能力。

当代生成模型已经能造出从未见过、却非常像训练数据的图像和文本。加上约束（一部分输出必须保持）就是 conditional generation：inpainting 要保留未遮挡像素，text completion 要接上已有前缀。大语言模型看起来像在回答问题，常常只是在补全「这段文档接下来最可能是什么」——它知道语言的统计，不等于理解答案的意义。

### 2.1 观测维数常常是虚高的

有意义的英文句子，远少于随机抽词拼出来的字符串。真实图像也只是随机 RGB 立方体里薄薄的一片，因为图像来自物理过程。一张人脸大约 42 块肌肉，同一人、同一光照下的大部分变化，用几十个数就能描述。

于是出现 **latent variables**：每个样本底下有一组更简单的内部坐标，深度网络负责内部坐标和观测之间的映射。Latent 的分布通常被设计得很简单。采样变成两步：先从简单分布抽 \(z\)，再让网络把它映到数据空间。

有了这套坐标，还可以做训练数据里没直接出现过的操作：找到两张真图各自的 \(z\)，在中间插值，再映回像素，得到既像两边、又看起来合法的过渡帧。

把 \(z\) 先缩成两个数，故事就画得出来。图 A 的内部坐标 \(z_A=(0,0)\)，图 B 的 \(z_B=(4,2)\)。直线插值走 \(\alpha\in[0,1]\)：

\[
z(\alpha)=(1-\alpha)z_A+\alpha z_B=(\,4\alpha,\,2\alpha\,).
\]

\(\alpha=0,0.5,1\) 分别落在 \(z=(0,0),\ (2,1),\ (4,2)\)。解码器 \(g\) 把每个 \(z\) 映回像素：两端是两张真图，中间那张训练集里没有，却往往仍然「像一张脸」。

```text
z-space (2 numbers)              pixel-space (via decoder g)

  B (4,2) *                      image B
           \
            * (2,1)  ----g---->  blend that was never a training photo
           /
  A (0,0) *                      image A
```

第 14、17 章会警告：latent 说的是「未观测」，不一定是压缩，更不一定是可拧的语义旋钮。中间点「看起来合法」，只说明 \(g\) 把这条线段附近都映到了数据流形上，不说明 \(\alpha\) 对应「微笑程度」。

### 2.2 结构化输出可以从这条路上借力

要学「caption \(\rightarrow\) 图」，不必直接从文本像素对像素地映射。可以先分别用 latent 解释文本和图像，再学两组内部坐标之间的关系。原书给了三个好处：输入输出都更低维，可能少要一些成对数据；任意合理的 \(z\) 都该像一张图，所以更不容易造出非图；在映射里加随机性，同一句 caption 可以对应多张图。

---

## 3. Reinforcement learning：动作会改下一时刻的世界

第三块引入 **agent**：它活在一个世界里，每个时间步可以做某些动作。动作改变系统状态，但不一定是确定性的。动作也可能产生 **reward**。目标是学会选动作，使平均回报高。

两件立刻和 supervised 不同的事：

**Temporal credit assignment。** 回报可能在动作很久之后才到，不容易把功记在哪一步上。

**Exploration vs exploitation。** 已经会拿中等回报了，是继续吃老本，还是试没试过的动作，看看有没有更好的路？

人形机器人过障碍：动作是关节运动，状态是姿态，回报是到达检查点。一次回报对应一长串动作，哪些关键、哪些无关，当时看不出来。下棋更极端：对手怎么应不是确定的；若只在终局给「赢/输」，整盘棋的 credit assignment 极难。机器人若发现「侧躺用一条腿蹭」也能前进，它就面对一个真实选择：继续蹭，还是试着站起来走——后者可能快得多，也可能先摔很多次。

深度网络在这里的一种用法，是 **policy network**：从观测到的世界状态映到动作。机器人是传感器 \(\rightarrow\) 关节；下棋是棋盘 \(\rightarrow\) 走子。第 19 章会把这句话展开：policy 不但决定现在怎么做，还决定下一批评训数据从哪来。

---

## 4. Ethics：工具已经在场，责任问题不能后置

原书把伦理放在第 1 章而不是书末附录，理由很直：深度学习会改变世界的幅度，至少不亚于电、内燃机、晶体管或互联网。医疗、设计、交通、教育、几乎所有商业都可能受益；伤害的潜力同样大。工程师习惯对结果过分乐观。下面五条是原书点名的切口，不是完整清单。

**Bias and fairness。** 用历史数据预测薪资，会把历史歧视再学一遍。把非白人脸超分得更白、让「律师」的生成图只出现男人，已经是新闻。算法决策用得轻率，会把已有偏见焊得更牢。

一个故意缩小的数字就能看见「再学一遍」怎么发生。历史表里两组人、同一套技能分 \(x=80\)：A 组过去平均薪资 \(20\) 万，B 组 \(16\) 万——差的是历史，不是 \(x\)。最小二乘如果只看见 \((x,y)\)，会学出「\(x=80\) 时预测大约 \(18\)」这种看起来中性的均值，也可能学出「分组特征」当捷径，把 B 继续压在 \(16\)。误差对模型是对称的平方；对申请人不是：B 组每人少 \(4\) 万，不是噪声，是被写进标签里的旧规则。公平问题在选数据的那一步就已经进场，不是训完再贴的补丁。

**Explainability。** 深度网络做决定，通常说不清依据。系统可以极大，无法靠检查权重理解。Explainable AI 目前比较成功的是局部解释：解释不了整个系统，但可以解释「这一次为什么这样判」。能否做出对用户甚至对作者完全透明的复杂决策系统，仍然未知。

**Weaponizing AI。** 重大技术最终都会直接或间接用于战争。AI 已经在军事场景里部署。

**Concentrating power。** 最有钱的公司投入 AI，不是因为慈悲。技术会把力量集中到控制它的少数组织。被自动化的工作更可能先打到低收入、低技能的人。乐观故事是工业革命最终缩短工时；诚实的句子是：大规模采用的社会效应，我们其实不知道。

**Existential risk。** 人类的主要存亡风险都来自技术：工业化驱动气候，物理研究给出核武器，交通和农业让疫情传得更快。AI 增加新的一层。最乐观：巨大权力落在所有者手里。最悲观：无法控制，甚至无法理解其动机。

这份名单远不完整。监视、虚假信息、隐私、欺诈、操纵市场、训练耗能，都已经有案例。互联网早期社区也没预见到假新闻、垃圾邮件、网暴、欺诈、政治操纵。原书把问题抛回读者：科学家对技术被怎么用负多少责？发展主要由资本主义驱动，法律和公益应用会明显滞后。准备为哪类组织工作？对方是真的能叫停可疑项目，还是只在 ethics-wash？

第 21 章会把这里的清单收成 alignment、misuse 和集体行动问题。现在只需留下这句：学这套工具的同时，就要把责任问题放在桌上。

---

## 5. 全书怎么走，这一章读到哪就算够

后面章节沿用这一章的三问：

```text
2–9   supervised 流水线：模型、损失、拟合、梯度、衡量、正则
10–13 把偏好写进结构：卷积、残差、Transformer、图网络
14–18 无监督生成：坐标系，然后 GAN / Flow / VAE / Diffusion
19    深度强化学习（只能点到为止）
20    为什么容易训、为什么能泛化、为什么要这么大、是否必须深
21    伦理，不再是第 1 章的速写
```

第 2 章要打开的黑箱是：所谓模型，究竟是一个方程还是一族方程？所谓训练，究竟在最小化什么？

---

## 6. 自测

1. AI、machine learning、deep learning 是三个同义词，还是包含关系？  
2. Supervised / unsupervised / RL 分别在问什么？为什么不是「有标签 → 没标签 → 更难」？  
3. 表格特征和自然语言输入，对「打乱次序」的态度差在哪？  
4. 训练（fitting）和 inference 各做哪一步？「老师」在数据里以什么形式出现？  
5. 语义分割的输出结构和英译法的输出结构，哪一种更绑在输入上？这件事为什么会通向 unsupervised？  
6. 会合成新样本，和会写 \(p(x)\)，是同一件事吗？  
7. Latent variable 解决的是「观测维数虚高」。从 latent 插值再映回数据，依赖哪两个已学会的映射？  
8. Temporal credit assignment 和 exploration–exploitation 为什么是 RL 里 supervised 不会直接遇到的两件事？  
9. Policy network 把什么映到什么？  
10. 用历史薪资数据训模型，最可能把哪一类伦理问题再学一遍？  
11. 局部解释能回答哪句话，不能回答哪句话？  
12. 原书为什么把 ethics 放在第 1 章，而不是等你会训网络了再谈？

---

## 7. 合上书再看一眼

机器学习是用数据拟合模型。深度网络只是一种特别能表示复杂关系、又相对容易搜索的模型，所以它同时出现在 supervised、unsupervised 和 RL 里。这三块不是难度梯子：一块要 \(x\mapsto y\)，一块要在没有 \(y\) 时描述或再造 \(x\)，一块要在动作会改世界的情况下拿长期回报。

输入可以是表格、文本、音频、图像或分子；输出也可以带自己的语法。当输出的语法不必跟输入走，无监督生成就开始成为 supervised 的盟友。内部坐标 \(z\) 是为了让「简单噪声 \(\rightarrow\) 复杂样本」可学。RL 则把时间引进来：现在的动作是下一批评训数据的来源。

这些能力已经大到会改社会结构。Bias、不可解释、武器化、权力集中、存亡风险，不是学完技术再贴的标签。

下一章不再把模型当黑箱。它用最简单的直线，把「一族函数、一个损失、一次下山」走完一遍。
