# 信息论学习指南：从信息量到交叉熵

## 1. 信息论研究什么

信息论研究的核心问题是：

> 如何用数学刻画不确定性、一次观察带来的信息，以及表示和传输信息所需的最少资源。

它不直接讨论一句话的文学意义，而主要研究：

- 一个事件发生前有多难预测；
- 事件发生后消除了多少不确定性；
- 数据最多能够压缩到多短；
- 有噪声的信道最多能可靠传输多少信息；
- 一个概率模型与真实数据分布相差多少。

## 2. 起源

现代信息论通常以 Claude Shannon 1948 年发表的论文 *A Mathematical Theory of Communication* 为起点。

当时要解决的是通信工程问题：

- 一条信道最多能传输多少信息？
- 数据能够压缩到什么程度？
- 存在噪声时能否可靠传输？
- 应该怎样设计编码？

Shannon 将通信过程抽象为：

```text
信息源 → 编码器 → 信道 → 解码器 → 接收者
                    ↑
                   噪声
```

在 Shannon 之前，Ralph Hartley 于 1928 年提出可以使用可能消息数量的对数衡量信息。Shannon 在此基础上引入事件概率，建立了完整的信息理论。

后来，Solomon Kullback 和 Richard Leibler 于 1951 年系统提出 KL 散度，用于衡量两个统计分布之间的信息差异。

---

# 第一部分：信息量

## 3. 一次事件带来多少信息

假设事件 $x$ 的概率是 $P(x)$，该事件发生时带来的信息量定义为：

$$
I(x)=-\log P(x)
$$

它也叫自信息或惊讶度。

核心思想是：

> 一个事件发生前越难预测，它真的发生以后提供的信息越多。

例如：

- “太阳明天升起”几乎必然发生，信息量很小；
- “某人中了头奖”概率极低，发生时信息量很大。

## 4. 为什么使用负对数

### 4.1 概率越小，信息越大

$$
P(x)\downarrow
\quad\Longrightarrow\quad
-\log P(x)\uparrow
$$

### 4.2 必然事件不提供新信息

如果 $P(x)=1$：

$$
I(x)=-\log1=0
$$

因为我们早已确定它会发生，观察到它没有消除任何不确定性。

### 4.3 独立事件的信息应该相加

如果 $x$ 和 $y$ 独立：

$$
P(x,y)=P(x)P(y)
$$

那么：

$$
I(x,y)
=
-\log[P(x)P(y)]
=
I(x)+I(y)
$$

对数把概率的乘法变成了信息量的加法。

## 5. 信息量的单位

单位由对数底数决定。

### 5.1 以 2 为底

$$
I(x)=-\log_2P(x)
$$

单位是 bit。

若事件概率为 $1/8$：

$$
I(x)=-\log_2\frac18=3\text{ bits}
$$

### 5.2 以自然常数 $e$ 为底

$$
I(x)=-\ln P(x)
$$

单位是 nat。机器学习损失通常使用自然对数，所以交叉熵的单位一般是 nat。

---

# 第二部分：信息熵

## 6. 从单个事件到平均不确定性

单个事件的信息量是：

$$
I(x)=-\log P(x)
$$

但事件发生前，我们不知道最终会观察到哪个结果。因此对所有可能结果的信息量求期望：

$$
H(P)
=
\mathbb E_{x\sim P}[-\log P(x)]
$$

离散情况下：

$$
\boxed{
H(P)=-\sum_xP(x)\log P(x)
}
$$

这就是信息熵，简称熵。

公式中两个 $P$ 的作用不同：

$$
-\sum_x
\underbrace{P(x)}_{\text{事件实际出现的频率}}
\underbrace{\log P(x)}_{\text{事件发生时的信息量}}
$$

## 7. 熵的两种核心含义

### 7.1 不确定性视角

> 熵是一个随机变量在观察之前的平均不确定性。

分布越均匀，越难预测，熵越高；分布越集中，越容易预测，熵越低。

### 7.2 编码视角

> 熵是使用最优编码表示该信息源时，每个符号所需的理论平均编码长度。

因此，熵不仅是抽象的不确定性，也对应实际的压缩极限。

## 8. 硬币例子

设正面概率为 $p$，反面概率为 $1-p$：

$$
H(p)
=
-p\log_2p-(1-p)\log_2(1-p)
$$

### 8.1 公平硬币

如果 $p=0.5$：

$$
H=1\text{ bit}
$$

公平硬币最难预测，因此熵最大。

### 8.2 严重偏向的硬币

如果 $p=0.99$：

$$
H\approx0.081\text{ bits}
$$

它几乎总是正面，所以不确定性较低。

### 8.3 必然事件

如果 $p=1$：

$$
H=0
$$

结果完全确定，不存在不确定性。

对于 $K$ 个类别，当所有类别等概率时：

$$
P(x=k)=\frac1K
$$

熵达到最大值：

$$
H_{\max}=\log K
$$

## 9. 熵与数据压缩

假设信息源产生四种字符：

| 字符 | 概率 | 编码 |
|---|---:|---|
| A | 0.5 | `0` |
| B | 0.25 | `10` |
| C | 0.125 | `110` |
| D | 0.125 | `111` |

常见字符使用短编码，罕见字符使用长编码。平均编码长度为：

$$
0.5\times1
+0.25\times2
+0.125\times3
+0.125\times3
=1.75\text{ bits}
$$

这个分布的熵也正好是 $1.75$ bits。

Shannon 源编码定理的核心思想是：

> 对足够长的数据，熵决定无损压缩能够达到的理论极限。

---

# 第三部分：交叉熵

## 10. 为什么需要第二个分布

只有真实分布 $P$ 时，我们讨论的是世界本身有多不确定，对应信息熵。

现实中，我们通常不知道 $P$，只能建立模型分布 $Q$：

- $P$：世界真正产生数据的规律；
- $Q$：我们对世界建立的概率模型。

现在的问题变成：

> 数据来自 $P$，但我们使用 $Q$ 进行预测或编码，会付出多少平均代价？

这就是交叉熵要回答的问题。

## 11. 交叉熵的定义

$$
\boxed{
H(P,Q)
=
-\sum_xP(x)\log Q(x)
}
$$

也可以写成：

$$
H(P,Q)
=
\mathbb E_{x\sim P}[-\log Q(x)]
$$

拆开来看：

$$
-\sum_x
\underbrace{P(x)}_{\text{真实事件出现的频率}}
\underbrace{\log Q(x)}_{\text{模型认为事件有多意外}}
$$

因此：

> 交叉熵是模型 $Q$ 面对真实数据 $P$ 时的平均惊讶程度。

## 12. 熵和交叉熵的区别

信息熵：

$$
H(P)=-\sum_xP(x)\log P(x)
$$

交叉熵：

$$
H(P,Q)=-\sum_xP(x)\log Q(x)
$$

区别在对数内部：

- 信息熵使用真实概率 $P(x)$；
- 交叉熵使用模型概率 $Q(x)$。

信息熵假设我们知道真实世界的规律；交叉熵允许模型与真实世界不一致。

通常：

$$
H(P,Q)\geq H(P)
$$

当且仅当 $Q=P$ 时取等号。

## 13. 天气例子

假设真实天气分布为：

$$
P=(0.8,0.2)
$$

也就是晴天概率为 $0.8$，雨天概率为 $0.2$。

### 13.1 完全正确的模型

$$
Q_1=(0.8,0.2)
$$

此时：

$$
H(P,Q_1)=H(P)\approx0.722\text{ bits}
$$

### 13.2 不太准确的模型

$$
Q_2=(0.6,0.4)
$$

此时：

$$
H(P,Q_2)\approx0.853\text{ bits}
$$

相比完美模型，多付出：

$$
0.853-0.722=0.131\text{ bits}
$$

### 13.3 判断相反的模型

$$
Q_3=(0.2,0.8)
$$

此时：

$$
H(P,Q_3)\approx1.922\text{ bits}
$$

模型与真实分布越不一致，交叉熵越大。

## 14. 交叉熵的编码含义

理想编码中，模型认为概率为 $Q(x)$ 的事件，编码长度约为：

$$
l_Q(x)=-\log_2Q(x)
$$

如果数据实际来自 $P$，但编码方案按照 $Q$ 设计，平均编码长度为：

$$
\sum_xP(x)[-\log_2Q(x)]
=
H(P,Q)
$$

所以交叉熵还表示：

> 数据来自 $P$，但使用按照 $Q$ 设计的编码方案时，每个符号平均需要多少比特。

当 $Q=P$ 时，编码方案与数据规律匹配；当 $Q\neq P$ 时，会产生额外编码开销。

---

# 第四部分：KL 散度

## 15. KL 散度的思想

我们已经知道：

- $H(P)$：使用真实分布时的最优平均编码长度；
- $H(P,Q)$：错误使用模型 $Q$ 时的平均编码长度。

两者之差就是 KL 散度：

$$
\boxed{
D_{\mathrm{KL}}(P\Vert Q)
=
H(P,Q)-H(P)
}
$$

展开为：

$$
D_{\mathrm{KL}}(P\Vert Q)
=
\sum_xP(x)\log\frac{P(x)}{Q(x)}
$$

因此：

> KL 散度表示因为使用了不准确的模型 $Q$，相比使用真实分布 $P$，平均多付出了多少信息代价。

核心关系是：

$$
\boxed{
H(P,Q)
=
H(P)
+
D_{\mathrm{KL}}(P\Vert Q)
}
$$

可以理解为：

$$
\boxed{
\text{模型的总预测代价}
=
\text{世界固有的不确定性}
+
\text{模型错误造成的额外代价}
}
$$

## 16. KL 散度的统计推断起源

Kullback 和 Leibler 研究的是：观测数据能在多大程度上帮助我们区分两个概率分布。

假设存在两个假说：

$$
H_0:\text{数据来自 }Q
$$

$$
H_1:\text{数据来自 }P
$$

观察到事件 $x$ 后，似然比为：

$$
\frac{P(x)}{Q(x)}
$$

对数似然比为：

$$
\log\frac{P(x)}{Q(x)}
$$

它表示一次观测为“数据来自 $P$ 而不是 $Q$”提供了多少证据。

如果数据确实来自 $P$，对这个证据求平均：

$$
\mathbb E_{x\sim P}
\left[
\log\frac{P(x)}{Q(x)}
\right]
$$

就得到 $D_{\mathrm{KL}}(P\Vert Q)$。

## 17. 为什么 KL 散度不是普通距离

KL 散度一般不对称：

$$
D_{\mathrm{KL}}(P\Vert Q)
\neq
D_{\mathrm{KL}}(Q\Vert P)
$$

原因是二者描述不同问题：

- $D_{\mathrm{KL}}(P\Vert Q)$：数据来自 $P$，却用 $Q$ 解释；
- $D_{\mathrm{KL}}(Q\Vert P)$：数据来自 $Q$，却用 $P$ 解释。

如果某个事件满足：

$$
P(x)>0,
\qquad
Q(x)=0
$$

那么：

$$
D_{\mathrm{KL}}(P\Vert Q)=+\infty
$$

因为真实世界可能发生该事件，但模型宣称它绝对不可能。

---

# 第五部分：与机器学习的关系

## 18. 交叉熵与最大似然

给定训练数据：

$$
x_1,x_2,\ldots,x_N
$$

模型分布为 $Q_\theta(x)$。最大似然估计要求：

$$
\hat\theta
=
\arg\max_\theta
\prod_{i=1}^{N}Q_\theta(x_i)
$$

取对数并转换为最小化问题：

$$
\hat\theta
=
\arg\min_\theta
\left[
-\frac1N\sum_{i=1}^{N}\log Q_\theta(x_i)
\right]
$$

训练数据形成经验分布 $P_{\mathrm{data}}$，上式就是：

$$
H(P_{\mathrm{data}},Q_\theta)
$$

因此：

$$
\boxed{
\text{最大化似然}
\equiv
\text{最小化负对数似然}
\equiv
\text{最小化经验交叉熵}
\equiv
\text{最小化 }D_{\mathrm{KL}}(P_{\mathrm{data}}\Vert Q_\theta)
}
$$

它们是统计学、优化和信息论对同一目标的不同表述。

## 19. 分类中的交叉熵

假设类别为：

$$
\{\text{苹果},\text{香蕉},\text{橘子}\}
$$

真实类别是苹果，one-hot 真实分布为：

$$
P=(1,0,0)
$$

模型预测：

$$
Q=(0.7,0.2,0.1)
$$

交叉熵为：

$$
H(P,Q)
=
-\sum_kP_k\log Q_k
$$

代入后：

$$
H(P,Q)
=
-(1\log0.7+0\log0.2+0\log0.1)
=
-\log0.7
$$

因此，对 one-hot 标签：

$$
\boxed{
L=-\log Q(\text{真实类别})
}
$$

真实类别概率越高，损失越小；模型越自信地排斥真实类别，损失越大。

## 20. 语言模型中的交叉熵

语言模型预测：

$$
Q_\theta(x_t\mid x_{<t})
$$

如果真实下一个 token 是 $x_t$，该位置的损失为：

$$
L_t=-\log Q_\theta(x_t\mid x_{<t})
$$

整个序列的损失为：

$$
L
=
-\sum_t\log Q_\theta(x_t\mid x_{<t})
$$

可以从三个角度理解：

- 概率论：提高真实文本的条件概率；
- 信息论：减少模型看到真实 token 时的平均惊讶；
- 编码论：减少使用模型分布编码真实文本所需的平均长度。

因此，一个好的语言模型也可以理解成一个好的文本压缩器。

## 21. 困惑度

语言模型平均交叉熵为：

$$
\overline L
=
-\frac1T\sum_t\log Q_\theta(x_t\mid x_{<t})
$$

困惑度定义为：

$$
\operatorname{PPL}=\exp(\overline L)
$$

若平均交叉熵为 $\ln K$：

$$
\operatorname{PPL}=K
$$

可以粗略理解为：模型在每个位置仿佛需要从 $K$ 个同等可能的候选中选择。困惑度越低，模型对真实文本越不惊讶。

不同 tokenizer、数据集或计分方法下的困惑度不能简单直接比较。

---

# 第六部分：条件熵与互信息

## 22. 条件熵

假设 $X$ 是天气，$Y$ 表示一个人是否带伞。

天气原本的不确定性为：

$$
H(X)
$$

知道是否带伞后，天气剩余的不确定性为：

$$
H(X\mid Y)
$$

定义为：

$$
H(X\mid Y)
=
-\sum_{x,y}P(x,y)\log P(x\mid y)
$$

其含义是：

> 已经知道 $Y$ 后，关于 $X$ 仍然保留的平均不确定性。

如果是否带伞与天气高度相关：

$$
H(X\mid Y)<H(X)
$$

如果二者完全无关：

$$
H(X\mid Y)=H(X)
$$

## 23. 互信息

知道 $Y$ 后，关于 $X$ 被消除的不确定性是：

$$
\boxed{
I(X;Y)=H(X)-H(X\mid Y)
}
$$

这就是互信息。

它也可以写成：

$$
I(X;Y)
=
D_{\mathrm{KL}}
\left(
P(X,Y)
\Vert
P(X)P(Y)
\right)
$$

它衡量联合分布与“假设两个变量独立”之间的差异。

如果 $X,Y$ 独立：

$$
P(X,Y)=P(X)P(Y)
$$

因此：

$$
I(X;Y)=0
$$

互信息可以刻画非线性依赖，不仅限于线性相关性。

---

# 第七部分：应用

## 24. 通信系统

- 信道容量；
- 噪声环境下的可靠传输；
- 纠错码；
- 调制和编码设计。

## 25. 数据压缩

- Huffman 编码；
- 算术编码；
- 图片、音频和视频压缩；
- 神经压缩模型。

## 26. 机器学习

- 分类交叉熵；
- 语言模型训练；
- KL 正则化；
- 变分自编码器；
- 知识蒸馏；
- 决策树的信息增益。

## 27. 特征和表示学习

- 使用互信息进行特征选择；
- 衡量表征与目标变量的依赖；
- 聚类和独立成分分析；
- 神经科学中的信号分析。

## 28. 安全与隐私

- 密码系统的信息泄漏；
- 匿名化；
- 差分隐私；
- 侧信道分析。

---

# 第八部分：概念总表

## 29. 关键概念

| 概念 | 公式 | 回答的问题 |
|---|---|---|
| 信息量 | $-\log P(x)$ | 这个具体事件有多意外？ |
| 信息熵 | $\mathbb E_P[-\log P(x)]$ | 真实世界平均有多不确定？ |
| 交叉熵 | $\mathbb E_P[-\log Q(x)]$ | 模型面对真实数据时平均有多惊讶？ |
| KL 散度 | $H(P,Q)-H(P)$ | 模型错误额外造成多少代价？ |
| 条件熵 | $H(X\mid Y)$ | 知道 $Y$ 后，$X$ 还剩多少不确定性？ |
| 互信息 | $H(X)-H(X\mid Y)$ | 知道 $Y$ 消除了多少关于 $X$ 的不确定性？ |

## 30. 最终心智模型

$$
\boxed{
\text{信息量}
=
\text{一次结果带来的惊讶}
}
$$

$$
\boxed{
\text{信息熵}
=
\text{按照真实分布观察结果时的平均惊讶}
}
$$

$$
\boxed{
\text{交叉熵}
=
\text{按照模型分布预测真实数据时的平均惊讶}
}
$$

$$
\boxed{
\text{KL 散度}
=
\text{模型不准确造成的额外平均惊讶}
}
$$

$$
\boxed{
\text{条件熵}
=
\text{获得部分信息后仍然剩余的不确定性}
}
$$

$$
\boxed{
\text{互信息}
=
\text{一个变量帮助消除的另一个变量的不确定性}
}
$$

整条主线可以概括为：

```text
世界本身存在不确定性
          ↓
         H(P)
          ↓ 使用模型 Q 进行预测
        H(P,Q)
          ↓ 减去不可避免的 H(P)
      KL(P || Q)
```

机器学习无法消除世界中真实存在的随机性。它能够做的是减少模型错误造成的额外代价，使模型分布 $Q$ 尽量接近真实数据分布 $P$。

