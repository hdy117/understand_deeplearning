# 第 4 章：深度神经网络（Deep Neural Networks）

> 书：《Understanding Deep Learning》Ch.4  
> 前置：第 3 章的浅层网已经能任意逼近连续函数；代价是 hidden units 可能要多到没法用。  
> 本章：把一个浅层网的输出送进另一个，深度怎样用折叠复制函数；以及浅和深在区域数、可训性、结构输入上差在哪。  
> 后续：函数族已经够用，第 5 章问怎样把「拟合得好不好」压成可最小化的标量。

---

## 0. 浅层已经够表达，为什么还要深

第 3 章结尾留下一个尴尬：宽度版 UAT 说，单隐层、够多 hidden units，就能任意逼近。那「deep」这个词从哪来的必要性？

原书的回答不是「深层更神秘」，而是几件可以分开看的事实：对某些函数，浅层需要的 hidden units 多到不现实；同样参数预算下，深层能造出多得多的线性区域；图像这类结构化输入几乎只能一层一层从局部收到全局；实践里中等深度也往往比同等规模的浅层更好训、更好泛化——最后这条机制并不清楚，第 20 章再问。

ReLU 下，浅层和深层描述的都是 piecewise linear 映射。差的不是「折不折」，是折怎样被复用。

调查链：

```text
浅层够宽就能逼近，但 hidden units 可能要爆炸
        │
        ▼
两个浅层网串联：第一个把输入折叠，第二个的函数被复制多份
        │
        ▼
这种串联是两隐层网络的特例（还更窄，因为权重被写成外积）
        │
        ▼
每一层 ReLU 再剪一次，给已有折线加新关节
        │
        ▼
深度、宽度是超参数：先选定，再在那个族里找 φ
        │
        ▼
矩阵记号把「线性–激活」写成可叠很多层的一行
        │
        ▼
浅 vs 深：区域/参数、depth efficiency、结构输入、训练与泛化
        │
        ▼
族已经很大 → 第 5 章：用损失选出其中一个成员
```

---

## 1. 先串联两个浅层网：折叠，然后复制

各三个 hidden units 的两个浅层网，第一个的输出 \(y\) 当作第二个的输入。ReLU 下，第二个本身最多四段。关键构造是：让第一个在 \(x\in[-1,1]\) 上做出三段正负交替的斜率，于是三段不同的 \(x\) 被映到同一段 \(y\in[-1,1]\)。第二个对这段 \(y\) 定义的函数，会被套用三次。整体最多九段——比六个 hidden units 的浅层网（最多七段）更多。

换一张空间的图：第一个网络把输入轴 **fold** 回去，多个 \(x\) 落到同一个 \(y\)；第二个在折叠后的空间上作用；再 unfold，同一段函数出现在被叠在一起的每一份里。二维输入同样发生：第一个网造出若干多边形区域，第二个在每个非平坦区域里再切一刀。

这就是深度的第一张直觉图：**不是多画一些彼此无关的折，而是把已经学会的折复制到输入空间的许多位置。** 复制带翻转和缩放，取决于第一网各段斜率的符号和大小。

---

## 2. 串联其实就是两隐层，而且两隐层更宽

第一个网的输出 \(y=\phi_0+\sum\phi_d h_d\) 对 \(h\) 是线性的；第二个网第一件事又是对 \(y\) 做线性。线性套线性还是线性。把 \(y\) 代进去，第二层 hidden units 直接写成第一层 \(h\) 的线性组合再激活：

\[
h'_k
=
a\bigl(\psi_{k0}+\psi_{k1}h_1+\psi_{k2}h_2+\psi_{k3}h_3\bigr).
\]

这正是两隐层网络。但若它真从两个浅层网复合而来，\(\psi\) 的九个斜率被锁成外积

\[
[\theta'_{11},\theta'_{21},\theta'_{31}]^\top
[\phi_1,\phi_2,\phi_3],
\]

不能任意取值。一般的两隐层网去掉这个约束，函数族 strictly 更大。所以：

> **「把一个浅层网送进另一个」\(\subset\)「两隐层深度网」。**

深度网不是两个现成模块的胶水，它允许第二层以任意线性方式混合第一层的激活。

---

## 3. 另一张图：clip，加关节，再组合

一般的两隐层、每层三单元，计算可以改用「剪」来读：

1. 第一层和浅层网一样，三个 ReLU 折线；
2. 第二层的 pre-activation 是这三个激活的新线性组合——相当于一个三输出浅层网，**关节位置相同**（第 3 章多输出那张图）；
3. 再过一次 ReLU：每条已有折线被再剪一次，出现新关节；
4. 输出是这些剪过的函数的线性组合。

折叠视角强调「同一段函数被复用」，不太强调新关节从哪来；clip 视角相反。两张图都只覆盖一部分机制。不要忘了，整张网仍然只是一个 \(x\mapsto y'\) 的方程，可以展开成一层套一层的 \(a(\cdot)\)，只是展开式几乎没法读。

**超参数**是训练开始前选定的量：层数 \(K\)（depth）、每层 hidden units \(D_1,\ldots,D_K\)（各层 width）。总 hidden units 数衡量 capacity。固定超参数之后，模型才是一族函数，\(\phi\) 选出其中一个成员。连超参数一起看，神经网络是 **一族函数族**。

现代网络可以一百多层、每层几千单元。更深不一定更好训：中等深度往往比浅层容易，再往上加层又变难，第 11 章的残差就是在修这件事。

---

## 4. 矩阵记号：往后每一章都用这套

一层是「仿射 + 按元素激活」。两隐层可以写成

\[
h = a(\theta_0+\theta x),
\qquad
h' = a(\psi_0+\Psi h),
\qquad
y' = \phi'_0+\phi'^\top h'.
\]

层一多，下标会爆炸。原书此后用：

- \(h_k\)：第 \(k\) 层激活向量；
- \(\beta_k\)：送进第 \(k+1\) 层的偏置；
- \(\Omega_k\)：从第 \(k\) 层到第 \(k+1\) 层的权重矩阵。

\(K\) 层网络：

\[
\begin{aligned}
h_1 &= a(\beta_0+\Omega_0 x),\\
h_2 &= a(\beta_1+\Omega_1 h_1),\\
&\vdots\\
h_K &= a(\beta_{K-1}+\Omega_{K-1}h_{K-1}),\\
y &= \beta_K+\Omega_K h_K.
\end{aligned}
\]

参数 \(\phi=\{\beta_k,\Omega_k\}_{k=0}^{K}\)。形状约定：第 \(k\) 层有 \(D_k\) 个单元，则 \(\beta_{k-1}\in\mathbb{R}^{D_k}\)，最后的 \(\beta_K\) 长度等于输出维 \(D_o\)；\(\Omega_0\) 是 \(D_1\times D_i\)，\(\Omega_K\) 是 \(D_o\times D_K\)，中间 \(\Omega_k\) 是 \(D_{k+1}\times D_k\)。

也可以压成一行：

\[
y
=
\beta_K
+
\Omega_K
a\bigl(\beta_{K-1}+\Omega_{K-1}a(\cdots a(\beta_0+\Omega_0 x)\cdots)\bigr).
\]

第 7 章的反传，就是在这条嵌套上从外往里复用中间量。ReLU 还有非负齐次性 \(\mathrm{ReLU}(\alpha z)=\alpha\,\mathrm{ReLU}(z)\)（\(\alpha>0\)）：权重可以整体缩放，只要偏置跟着调、最后再把尺度乘回来。函数族因此有冗余，损失不会只有一个孤立的「最佳 \(\phi\)」。

---

## 5. 浅 vs 深：五件要分开记账的事

两者都能在容量足够时任意逼近：深层可以让某一层近似恒等，退化成浅层，宽度版 UAT 就传过来了。还有深度版 UAT：ReLU 网、每层至少 \(D_i+4\) 个单元，层数够多时也可以逼近任意 Lebesgue 可积函数（Lu et al., 2017）。所以「能不能表示」几乎不是分界线。分界线在效率、结构和训练。

### 5.1 每个参数能买多少线性区域

标量到标量：浅层 \(D>2\) 个 hidden units，最多 \(D+1\) 段，参数 \(3D+1\)。深层 \(K\) 层、每层 \(D>2\)，最多 \((D+1)^K\) 段，参数 \(3D+1+(K-1)D(D+1)\)。固定参数预算，深度把区域数抬得极快；输入维升高，效果更猛。\(K=5,D=10\) 约 471 个参数就能到 \(161{,}051\) 段；\(D_i=10,K=5,D=50\) 约 10,801 个参数，区域数可以超过 \(10^{40}\)。

但区域多不等于灵活无约束。折叠会带来对称和依赖：许多段其实是同一段函数的复制。除非真实世界的映射也有类似的组合结构，否则「区域多」只是账面上的。

若每层宽度相同、且 \(D\) 是 \(D_i\) 的整数倍，最大区域数可以写成

\[
N_r
=
\Bigl(\frac{D}{D_i}+1\Bigr)^{D_i(K-1)}
\cdot
\sum_{j=0}^{D_i}\binom{D}{j}.
\]

前一项是前 \(K-1\) 层对输入空间的反复折叠（每个输入维大约要分到 \(D/D_i\) 个单元去折），后一项是最后一层这个浅层网能切出的区域。

### 5.2 Depth efficiency

存在一些函数，深层用多项式级资源就能逼近，浅层却要指数多的 hidden units。这叫 **depth efficiency**。Telgarsky、Eldan & Shamir 以及后续工作给出了具体构造。反过来也有 **width efficiency**：有些宽浅层网，窄网络要用多项式深度才能追上，这个下界比深度那边的指数弱，暗示深度通常更值钱。

同样的保留：不清楚真实要拟合的函数是不是落在这些「深层指数占优」的类里。

### 5.3 大而有结构的输入

全连接意味着上一层每个单元影响下一层每个单元。百万像素的图会让参数爆炸，而且我们本来就希望图的不同位置用同一套探测器——没必要在每个坐标独立学会「猫」。做法是局部并行处理，再一层层把信息收到更大的区域。这种 local-to-global 几乎写不出「单隐层」的形式。第 10 章的卷积把这件事变成权重共享。

### 5.4 训练和泛化

实践里，中等深度往往比浅层更好拟合（原书指向图 20.2）。一种假说是过参数的深层有一大片差不多好的解，容易撞上。层再多，训练又变难，需要第 11 章那些技巧。泛化也是深层在多数任务上赢，最好的结果常用几十到几百层。两件事都还没有被真正理解，第 20 章专门回去问。

---

## 6. 自测

1. 宽度版 UAT 已经成立，原书还要深度的哪几条彼此独立的理由？  
2. 第一个浅层网怎样「折叠」输入，才能让第二个网的函数被复制三遍？  
3. 为什么两个浅层网的复合只是两隐层网的一个子集？外积约束锁住了什么？  
4. Clip 视角里，第二层 ReLU 相对第一层新提供了什么？  
5. 超参数和参数各在何时选定？为什么说神经网络是「一族函数族」？  
6. \(\Omega_k\) 和 \(\beta_k\) 的形状怎样由 \(D_k,D_{k+1},D_i,D_o\) 决定？  
7. 把网络写成一行嵌套 \(a(\cdot)\)，对第 7 章的反传意味着计算要从哪一头开始？  
8. ReLU 的非负齐次性如何说明「最佳 \(\phi\)」不会是孤立的一点？  
9. 标量深层网最多 \((D+1)^K\) 段。为什么这不等于「灵活无约束」？  
10. Depth efficiency 说的是存在性构造，还是对真实数据的定理？  
11. 全连接处理图像时，哪两件事同时爆炸？第 10 章准备用什么写进结构？  
12. 「中等深度比浅层好训，再深又变难」把哪些章的缺口已经预支出去了？

---

## 7. 合上书再看一眼

深度不是把浅层网多堆几个无关的折。第一层把输入空间折叠，后面的层在折叠后的空间上作用，同一段 piecewise linear 函数被复制、翻转、再剪出新关节。一般的多层网比「两个浅层网胶水」更宽，因为层与层之间的线性混合不必是外积。

浅层和深层都能任意逼近。深度的实际优势写在三本账上：同样参数买到的区域更多（但带对称），某些函数上指数级更省，以及局部到全局的结构几乎必须分层。训练好不好、泛化好不好，目前更多是现象而不是定理。

模型现在是一族很大的 piecewise linear 函数。下一章不再扩大这个族，而问：给定训练数据，怎样构造一个标量，告诉下山该朝哪走。
