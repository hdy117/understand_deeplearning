# 第 2 章：监督学习（Supervised Learning）

> 书：《Understanding Deep Learning》Ch.2  
> 前置：第 1 章把 supervised 说成「用标签对，在一族函数里挑一条」。  
> 本章：把那句话拆成记号、损失和训练；用一条直线走完整个循环。  
> 后续：直线不够弯，第 3 章换成 piecewise linear 的浅层网络。

---

## 0. 黑箱里其实只有三件事

第 1 章把模型当成「吃向量、吐向量」的盒子。本章打开盒子，里面只有三件要同时看清的东西：

```text
模型：带参数的函数 y = f(x, φ)     ← 一族关系，φ 选出其中一条
损失：L[φ] 把「预测得有多差」压成标量
训练：找 φ̂ = argmin_φ L[φ]
```

Inference 是已知 \(\phi\) 之后算 \(y\)。Training 是已知成对数据 \(\{(x_i,y_i)\}\) 之后改 \(\phi\)。两件事方向相反，却共用同一个方程。

母问题：

> **所谓「学会了」，在最简单的直线模型里究竟指哪一步？哪些部分换了深度网络也不会变？**

调查链：

```text
模型是函数族，不是一条固定公式
        │
        ▼
参数 φ 选出族里的某一个成员
        │
        ▼
损失用训练对衡量这个成员有多差
        │
        ▼
直线 + 平方误差：损失是 φ 的曲面，最低点对应最贴数据的线
        │
        ▼
训练 = 从随机起点沿梯度下山（直线其实有闭式解，下山是为后面铺路）
        │
        ▼
还要在没见过的 test 上检验；直线可能欠拟合，更弯的模型可能过拟合
        │
        ▼
直线描述不了弯曲关系 → 第 3 章
```

---

## 1. 模型：\(y=f(x,\phi)\) 是一族映射

输入 \(x\)、输出 \(y\) 先假定是固定长度、固定次序的向量。二手 Prius 的例子里，\(x\) 总是先年龄后里程，\(y\) 是估价。这种没有内部拓扑的数据叫 structured / tabular data。

Inference 写出来只有一行：

\[
y = f(x).
\]

方程里其实还有参数 \(\phi\)。不同的 \(\phi\) 给出不同的输入–输出关系，所以必须写成：

\[
y = f(x,\phi).
\]

\(f\) 的形式是固定的（本章是直线，后面是网络）；被数据选中的是 \(\phi\)。训练数据集是 \(I\) 对 \(\{x_i,y_i\}\)。我们希望同一套 \(\phi\) 把每个训练输入送到它对应的输出附近。偏差被压成一个标量 \(L[\phi]\)，叫 **loss**。训练就是：

\[
\hat\phi = \arg\min_\phi L[\phi].
\]

损失其实也依赖数据集，完整写法是 \(L[\{x_i,y_i\},\phi]\)，通常省略。损失小，只说明训练对拟合得好。能不能用到没见过的输入上，要另拿 test data 看——第 8 章会把这句话升级成 population risk。

---

## 2. 最简单的族：一条直线

一维线性回归：

\[
y = f(x,\phi) = \phi_0 + \phi_1 x.
\]

两个参数：截距 \(\phi_0\)，斜率 \(\phi_1\)。所有可能的直线构成函数族；选定一对 \((\phi_0,\phi_1)\)，才得到族里那一条。换一组参数，预测整条移动。第 1 章里「青线家族」在这里变成可以写下来的两个数。

这个模型浅得几乎不像机器学习。原书选它，是因为后面每一步——损失、曲面、下山、测试——在直线上都能看见，换深度网络时这些步骤一个都不会少。

---

## 3. 损失：把「线离点有多远」加成一个数

训练点是 \(\{x_i,y_i\}_{i=1}^{I}\)。模型在 \(x_i\) 处的高度是 \(f(x_i,\phi)\)，和真实 \(y_i\) 之间有一段竖直偏差。原书把总偏差取成平方和：

\[
L[\phi]
=
\sum_{i=1}^{I}
\bigl(f(x_i,\phi)-y_i\bigr)^2
=
\sum_{i=1}^{I}
(\phi_0+\phi_1 x_i-y_i)^2.
\]

平方做了两件事：方向消失（线在点上或点下同样罚）；大偏差被额外加重。第 5 章会说明，这其实是「输出服从固定方差的 Gaussian」时的负对数似然。现在只需接受：我们需要一个能比较「哪组 \(\phi\) 更好」的标量，平方和是其中一个能画出来的选择。

因为最好的参数最小化这个式子，它叫 **least-squares loss**。\(L\) 随 \(\phi\) 变，所以也叫 **loss function** / **cost function**。只有两个参数时，每个 \((\phi_0,\phi_1)\) 对应曲面上一个高度，可以画成三维表面，也可以画成俯视热图。最低点就是「最贴这 12 个点的那条线」。

四个点就够把「哪条线赢」算完，不必先画曲面。数据：

\[
(x,y)\in\{(1,2),\ (2,3),\ (3,5),\ (4,6)\}.
\]

候选 A：\(\phi_0=0,\phi_1=1.5\)，即 \(y=1.5x\)。候选 B：\(\phi_0=0.5,\phi_1=1.4\)，即 \(y=0.5+1.4x\)。

| \(x\) | \(y\) | A 的 \(\hat y=1.5x\) | A 的残差\(^2\) | B 的 \(\hat y=0.5+1.4x\) | B 的残差\(^2\) |
|---|---|---|---|---|---|
| \(1\) | \(2\) | \(1.5\) | \(0.25\) | \(1.9\) | \(0.01\) |
| \(2\) | \(3\) | \(3.0\) | \(0\) | \(3.3\) | \(0.09\) |
| \(3\) | \(5\) | \(4.5\) | \(0.25\) | \(4.7\) | \(0.09\) |
| \(4\) | \(6\) | \(6.0\) | \(0\) | \(6.1\) | \(0.01\) |
| 合计 \(L\) | | | \(0.50\) | | \(0.20\) |

B 的 \(L=0.20\) 小于 A 的 \(0.50\)，所以在这四个点、这个平方尺子上，B 赢。没有第三种神秘标准：赢的就是把四段竖直偏差平方加起来更小的那一组 \(\phi\)。闭式最小二乘还会再找到一个可能比 B 更低的点；这里只需要看见 \(L\) 是可以逐点算出来的数，不是观感。

术语上有人把单点那一项叫 loss、把求和后的总量叫 cost；本书和多数机器学习文献混用。Cost 还可以含与单个数据点无关的项（第 9 章的显式正则）。更宽的词是 **objective function**：任何要被最大或最小化的函数。

---

## 4. 训练：在曲面上往山下走

找到使 \(L\) 最小的参数，叫 model fitting / training / learning。基本方法：随机初始化，测当前位置的梯度，沿最陡下山的方向走一步，再测、再走，直到梯度水平。

对线性回归，其实可以把 \(\partial L/\partial\phi_0\) 和 \(\partial L/\partial\phi_1\) 设为零，闭式解出 \(\phi\)。原书仍然画下山，是因为后面的网络既没有闭式解，参数也多到不能把损失对每个组合都算一遍。**迭代拟合**才是通用手续。第 6 章把这一步写成 \(\phi\leftarrow\phi-\alpha\nabla_\phi L\)；第 7 章才回答梯度怎么高效算出来。

一个容易混的点：下山走的是 **参数空间**，不是数据空间。图上的「位置 0 到位置 4」是 \((\phi_0,\phi_1)\) 平面上的点，每一个点对应数据平面上的一条线。损失下降，线才看起来更贴点。

把上一节的 A、B 画进两张平面，混就不起来了。参数平面上 A 是点 \((0,1.5)\)，B 是点 \((0.5,1.4)\)，高度分别是 \(L=0.50\) 和 \(L=0.20\)。数据平面上它们是两条不同的线，穿过同一批橙点：

```text
parameter space (phi0, phi1)          data space (x, y)

  phi1                                y
   ^                                   ^
1.5 * A  L=0.50                    6 - *           *  points
   |                                   |    B -----*
1.4 * B  L=0.20                    4 - |  /
   |                                   | / A
   +-------> phi0                  2 - *
        0    0.5                       +-------> x
                                       1  2  3  4
```

梯度下降移动的是左边那个点，不是右边把橙点挪走。左边走一步 \((\phi_0,\phi_1)\leftarrow(\phi_0,\phi_1)-\alpha\nabla L\)，右边整条线跟着改斜率、改截距。\(L\) 是左边的高度、右边四段竖线平方和，同一件事的两张脸。

---

## 5. 测试：训练点上的小损失不是终点

训完之后，把损失拿到一份没参加训练的 test data 上再算一遍。泛化得好不好，取决于训练数据有多完整、有多代表性，也取决于模型有多能弯。

**Underfitting：** 模型太简单，连训练数据里的真实关系都描述不了——一条直线拟合波浪就是这个病。

**Overfitting：** 模型太能弯，把训练集里非典型的统计起伏也写进方程，到新点上就会给出古怪预测。

三个训练点就能把两侧都算出来。真实关系接近 \(y=x\)，测量带一点噪声：训练对是 \((1,1.0),\ (2,2.2),\ (3,2.9)\)，另有一个没见过的 test 点 \((4,4.0)\)。

直线族 \(y=\phi_0+\phi_1 x\)。凑一组还算老实的参数 \(\phi_0=0.1,\phi_1=0.95\)，训练预测 \(1.05,2.00,2.95\)，平方损失

\[
(1.05-1)^2+(2-2.2)^2+(2.95-2.9)^2=0.0025+0.04+0.0025=0.045,
\]

test 上 \((0.1+0.95\cdot 4-4)^2=(-0.1)^2=0.01\)，还在点旁边。

再换一个「过能弯」的故事：硬记住三个训练点的二次 \(y=-0.25x^2+1.95x-0.7\)（过 \((1,1),\ (2,2.2),\ (3,2.9)\)）。训练损失是 \(0\)，看起来完美。代入 \(x=4\)：

\[
-0.25\cdot 16+1.95\cdot 4-0.7=-4+7.8-0.7=3.1,
\]

test 平方误差 \((3.1-4)^2=0.81\)，已经是直线那个 \(0.01\) 的八十倍。训练上更小，测试上更糟——过拟合就是这个交叉。欠拟合则是另一头：若死活只用水平线 \(y=2\)，训练损失 \((1-2)^2+(2.2-2)^2+(2.9-2)^2=1+0.04+0.81=1.85\)，连波浪的上升趋势都没抓住，test 上 \((2-4)^2=4\) 一样差。直线在这个玩具里几乎刚好；真实问题里「刚好」很少预先知道，所以才要第 8 章那份没见过的数据。

第 8 章会把这两句话收进 Bias–Variance；第 9 章会问：很多函数都能把训练损失压到零，学习系统凭什么选其中一个。本章只需留下缺口：直线可能太硬，下一章开始把族变软。

---

## 6. 一条容易走错的岔路：generative 版本的直线

本章的 \(y=f(x,\phi)\) 是 **discriminative**：从测量 \(x\) 直接预测 \(y\)。也可以反过来建 **generative** 模型 \(x=g(y,\phi')\)，再反解 \(y=g^{-1}(x,\phi')\) 做推理。好处是能把「数据怎么生成」的先验写进 \(g\)（车的三维几何、光照）；坏处是反解可能很难，而且对同一组 \(\{x_i,y_i\}\)，两条路拟合出的直线一般 **不是同一条**——最小化为「\(y\) 方向的误差」和「\(x\) 方向的误差」本来就不同。

现代深度学习几乎走 discriminative：用大量数据把灵活的 \(x\mapsto y\) 直接学出来，通常压过「把物理先验写进生成方程」的优势。第 14 章的 generative model 是另一件事：那里要生成的是 \(x\) 本身，不是给 \(y\) 当中间变量。

---

## 7. 自测

1. Inference 和 training 共用哪个方程？它们各自把什么当已知、什么当未知？  
2. 为什么必须写 \(y=f(x,\phi)\)，而不是 \(y=f(x)\)？  
3. \(\hat\phi=\arg\min_\phi L[\phi]\) 最小化的是对谁的误差？为什么还要 test data？  
4. 一维线性回归的函数族是什么？\(\phi_0\) 和 \(\phi_1\) 各移动这条线的哪一部分？  
5. 平方损失为什么让「线在点上还是点下」变得不重要？第 5 章会给它什么概率解释？  
6. 损失曲面的坐标轴是数据 \((x,y)\) 还是参数 \((\phi_0,\phi_1)\)？  
7. 线性回归既然有闭式解，为什么原书仍要讲「沿梯度下山」？  
8. Underfitting 和 overfitting 各是「模型表达能力」相对「真实关系」的哪一侧？  
9. Discriminative 的 \(y=f(x)\) 和 generative 的 \(x=g(y)\) 最小化的误差方向有何不同？  
10. 把单个数据项叫 loss、把求和叫 cost，原书采取什么态度？第 9 章会让 cost 里多出什么？

---

## 8. 合上书再看一眼

Supervised learning 被这一章钉成三件事：一个带参数的函数族、一个衡量族成员与训练对有多不贴的标量、一次在参数空间里找标量最低点的搜索。直线 \(\phi_0+\phi_1 x\) 把三件事都暴露出来：平方和是损失，\((\phi_0,\phi_1)\) 平面是曲面，下山是训练，另一份数据是测试。

直线的缺点同样暴露得干净：它只能是直的。真实关系一弯曲，再好的下山也只是在一个太小的家族里挑。下一章只多做一件事：在「线性」中间插入非线性，让同一套训练手续面对一族 piecewise linear 函数。
